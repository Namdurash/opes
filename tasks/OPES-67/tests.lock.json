{
  "schema": 1,
  "plan_sha256": "8a3b9aca59432d0c5fef403231a883c576ae6819b419781c341e00b65c76583c",
  "mode": "per-test",
  "mode_reason": "",
  "at": "2026-09-22T20:40:20Z",
  "tests": {
    "src/services/monobank/MonobankTokenService.test.ts": "2b86d53adb67d61581eb07a640e98e3c5f4f8a5fb4a551bfdb4b872286b13374",
    "src/services/monobank/migrateMonobankSecrets.test.ts": "eae2f4d27f31a923a386e1b7adb2807550976d0fb96ce592e173b97a9312c5d1",
    "src/services/secret-storage/SecretStore.test.ts": "1203ab0fc67800e43d1243ff98791d0f75b93c6d5809005ceed7eadb9fdcfa4a",
    "src/services/secret-storage/encryptedStore.test.ts": "347cfbb90d81a830bfce97ac09ca04bae3e2ab68d883c11009ace29cc6b23a2e",
    "test/CLAUDE.md": "ac800e4930195d17ec29479646499bd6f2a00e050d17b05ba59662945003444d",
    "test/db/createTestDatabase.ts": "3980e511bdfadbf7191d2f9aacefe87acf2a1f72664bcf87b0c85c3472f256b9",
    "test/db/index.ts": "72fd8c0649ae1635bd9d334f06bea079c466e7b34c554b3d6d02f74ce2790005",
    "test/doubles/cardsRepository.ts": "951c699b99c1ebbf1c1ce804b9bca35352ef2e4273d849bcc324b051d2774195",
    "test/doubles/index.ts": "2600505446129e2b078a94ebc0011386062c6c29dea8e09d805777baf8baab5a",
    "test/factories/card.ts": "848ef38faae77d256265132657369a0398784c0bf9ba332de1d52bf2e070a511",
    "test/factories/index.ts": "bdbfee3add31c224b058f925dd235b65b91ad6989097b9e9e46086e53115cf50",
    "test/factories/transaction.ts": "8aaa65083d628bd14f5b3711ab860fc131a8d9142a419220b27beb2b93d3eca6",
    "test/index.ts": "e6cabbb61f2984dcc29c2f16e3b86fef67c01ffb58767c10f063c69a405d43c4",
    "test/setup.js": "d816ef0a8996f218d7cd0c8830d995613f8f6149eaf61b19bb2eb672e7a6f70e"
  },
  "covering": [
    "createEncryptedStore instances AC-009 — enumerates both keys held in the encrypted store::createEncryptedStore instances AC-009 — enumerates both keys held in the encrypted store",
    "SecretStore.purgeDeletedRecords AC-006 — wipes nothing when a surviving value cannot be read for the snapshot::SecretStore.purgeDeletedRecords AC-006 — wipes nothing when a surviving value cannot be read for the snapshot",
    "SecretStore.purgeDeletedRecords AC-007 — is not reached by delete on an absent key::SecretStore.purgeDeletedRecords AC-007 — is not reached by delete on an absent key",
    "SecretStore.purgeDeletedRecords AC-008 — reads back a value written after the rebuild::SecretStore.purgeDeletedRecords AC-008 — reads back a value written after the rebuild",
    "SecretStore rebuilt by MonobankTokenService.clear AC-003 — keeps a third-party secret readable across a purging clear()::SecretStore rebuilt by MonobankTokenService.clear AC-003 — keeps a third-party secret readable across a purging clear()",
    "SecretStore rebuilt by MonobankTokenService.clear AC-005 — keeps a third-party secret readable when the rebuild throws::SecretStore rebuilt by MonobankTokenService.clear AC-005 — keeps a third-party secret readable when the rebuild throws",
    "MonobankTokenService.clear and the residue a delete leaves AC-001 — rebuilds the secret store exactly once::MonobankTokenService.clear and the residue a delete leaves AC-001 — rebuilds the secret store exactly once",
    "MonobankTokenService.clear and the residue a delete leaves AC-002 — rebuilds only after both deletes have gone through::MonobankTokenService.clear and the residue a delete leaves AC-002 — rebuilds only after both deletes have gone through",
    "MonobankTokenService.clear and the residue a delete leaves AC-004 — lets a failed rebuild reject rather than swallowing it::MonobankTokenService.clear and the residue a delete leaves AC-004 — lets a failed rebuild reject rather than swallowing it"
  ],
  "green_at_freeze": [
    "migrateMonobankSecrets on a fresh install AC-001 — writes nothing to the encrypted store when no plaintext copy exists::migrateMonobankSecrets on a fresh install AC-001 — writes nothing to the encrypted store when no plaintext copy exists",
    "migrateMonobankSecrets with a legacy plaintext copy AC-002 — copies the plaintext token into the encrypted store::migrateMonobankSecrets with a legacy plaintext copy AC-002 — copies the plaintext token into the encrypted store",
    "migrateMonobankSecrets with a legacy plaintext copy AC-003 — copies the plaintext client name into the encrypted store::migrateMonobankSecrets with a legacy plaintext copy AC-003 — copies the plaintext client name into the encrypted store",
    "migrateMonobankSecrets with a legacy plaintext copy AC-004 — deletes the plaintext token once it is safely across::migrateMonobankSecrets with a legacy plaintext copy AC-004 — deletes the plaintext token once it is safely across",
    "migrateMonobankSecrets with a legacy plaintext copy AC-005 — deletes the plaintext client name once it is safely across::migrateMonobankSecrets with a legacy plaintext copy AC-005 — deletes the plaintext client name once it is safely across",
    "migrateMonobankSecrets — the read-back gate before the destructive step AC-006 — deletes the plaintext token immediately after reading it back::migrateMonobankSecrets — the read-back gate before the destructive step AC-006 — deletes the plaintext token immediately after reading it back",
    "migrateMonobankSecrets — the read-back gate before the destructive step AC-007 — deletes nothing when the read-back differs from what was written::migrateMonobankSecrets — the read-back gate before the destructive step AC-007 — deletes nothing when the read-back differs from what was written",
    "migrateMonobankSecrets when the encrypted store already holds a value AC-008 — leaves the encrypted token untouched rather than overwriting it::migrateMonobankSecrets when the encrypted store already holds a value AC-008 — leaves the encrypted token untouched rather than overwriting it",
    "migrateMonobankSecrets when the encrypted store already holds a value AC-009 — still deletes the stale plaintext token::migrateMonobankSecrets when the encrypted store already holds a value AC-009 — still deletes the stale plaintext token",
    "migrateMonobankSecrets when one key fails AC-010 — migrates the token even though the client name write rejects::migrateMonobankSecrets when one key fails AC-010 — migrates the token even though the client name write rejects",
    "migrateMonobankSecrets when one key fails AC-011 — leaves the plaintext copy of the failed key where it is::migrateMonobankSecrets when one key fails AC-011 — leaves the plaintext copy of the failed key where it is",
    "migrateMonobankSecrets when one key fails AC-012 — resolves rather than rejecting when the encrypted store is unusable::migrateMonobankSecrets when one key fails AC-012 — resolves rather than rejecting when the encrypted store is unusable",
    "migrateMonobankSecrets run more than once AC-013 — retries a key whose earlier run failed::migrateMonobankSecrets run more than once AC-013 — retries a key whose earlier run failed",
    "migrateMonobankSecrets run more than once AC-014 — writes each key exactly once across two runs::migrateMonobankSecrets run more than once AC-014 — writes each key exactly once across two runs",
    "migrateMonobankSecrets and its plaintext neighbours AC-015 — leaves the selected-account ids in plaintext untouched::migrateMonobankSecrets and its plaintext neighbours AC-015 — leaves the selected-account ids in plaintext untouched",
    "migrateMonobankSecrets and the bytes left behind by a delete AC-017 — rebuilds the plaintext store once it has deleted something::migrateMonobankSecrets and the bytes left behind by a delete AC-017 — rebuilds the plaintext store once it has deleted something",
    "migrateMonobankSecrets and the bytes left behind by a delete AC-017 — rebuilds only after the last delete, never between the two keys::migrateMonobankSecrets and the bytes left behind by a delete AC-017 — rebuilds only after the last delete, never between the two keys",
    "migrateMonobankSecrets and the bytes left behind by a delete AC-018 — rebuilds nothing on a fresh install, where it deleted nothing::migrateMonobankSecrets and the bytes left behind by a delete AC-018 — rebuilds nothing on a fresh install, where it deleted nothing",
    "migrateMonobankSecrets and the bytes left behind by a delete AC-018 — rebuilds nothing when the read-back failed and nothing was deleted::migrateMonobankSecrets and the bytes left behind by a delete AC-018 — rebuilds nothing when the read-back failed and nothing was deleted",
    "migrateMonobankSecrets and the bytes left behind by a delete AC-019 — a rebuild that throws does not fail the migration::migrateMonobankSecrets and the bytes left behind by a delete AC-019 — a rebuild that throws does not fail the migration",
    "src/services/monobank/CLAUDE.md AC-016 — documents the legacy plaintext migration::src/services/monobank/CLAUDE.md AC-016 — documents the legacy plaintext migration",
    "SecretStore resolves null when nothing is stored under the key::SecretStore resolves null when nothing is stored under the key",
    "SecretStore round-trips a value through set then get::SecretStore round-trips a value through set then get",
    "SecretStore removes a stored value on delete::SecretStore removes a stored value on delete",
    "SecretStore is a silent no-op deleting an absent key::SecretStore is a silent no-op deleting an absent key",
    "SecretStore rejects get when the Keychain read fails transiently::SecretStore rejects get when the Keychain read fails transiently",
    "SecretStore generates the encryption key exactly once for concurrent first callers::SecretStore generates the encryption key exactly once for concurrent first callers",
    "SecretStore retries bootstrap and resolves after a transient failure recovers::SecretStore retries bootstrap and resolves after a transient failure recovers",
    "SecretStore rejects set when the encryption-key write fails::SecretStore rejects set when the encryption-key write fails",
    "SecretStore never writes plaintext when the encryption-key write fails::SecretStore never writes plaintext when the encryption-key write fails",
    "SecretStore regenerates the key and wipes stale ciphertext on a genuinely absent entry::SecretStore regenerates the key and wipes stale ciphertext on a genuinely absent entry",
    "SecretStore preserves the stored value across a transient Keychain read failure::SecretStore preserves the stored value across a transient Keychain read failure",
    "cryptoKey.generateKey produces a 32-byte raw key before base64 encoding::cryptoKey.generateKey produces a 32-byte raw key before base64 encoding",
    "cryptoKey.generateKey never draws key material from Math.random::cryptoKey.generateKey never draws key material from Math.random",
    "keychainKeyStore.writeKey writes the key device-only, not synchronizable to iCloud::keychainKeyStore.writeKey writes the key device-only, not synchronizable to iCloud",
    "package.json declares react-native-keychain as a dependency::package.json declares react-native-keychain as a dependency",
    "MonobankTokenService.save AC-001 — writes the token argument under monobank_personal_token::MonobankTokenService.save AC-001 — writes the token argument under monobank_personal_token",
    "MonobankTokenService.save AC-002 — writes the client name argument under monobank_client_name::MonobankTokenService.save AC-002 — writes the client name argument under monobank_client_name",
    "MonobankTokenService.save AC-008 — lets a rejected write reach the caller unchanged::MonobankTokenService.save AC-008 — lets a rejected write reach the caller unchanged",
    "MonobankTokenService.get AC-003 — resolves the token held in the secret store::MonobankTokenService.get AC-003 — resolves the token held in the secret store",
    "MonobankTokenService.get AC-004 — resolves the client name held in the secret store::MonobankTokenService.get AC-004 — resolves the client name held in the secret store",
    "MonobankTokenService.get AC-005 — resolves null rather than falling back to the plaintext copy::MonobankTokenService.get AC-005 — resolves null rather than falling back to the plaintext copy",
    "MonobankTokenService.get AC-009 — lets a rejected read reach the caller unchanged::MonobankTokenService.get AC-009 — lets a rejected read reach the caller unchanged",
    "MonobankTokenService.clear AC-006 — deletes monobank_personal_token::MonobankTokenService.clear AC-006 — deletes monobank_personal_token",
    "MonobankTokenService.clear AC-007 — deletes monobank_client_name::MonobankTokenService.clear AC-007 — deletes monobank_client_name",
    "the repository after the await propagation AC-013 — type-checks with every awaited call site in place::the repository after the await propagation AC-013 — type-checks with every awaited call site in place",
    "src/services/monobank/CLAUDE.md AC-014 — documents the token as living in secret-storage::src/services/monobank/CLAUDE.md AC-014 — documents the token as living in secret-storage"
  ],
  "impl_frozen": {
    "src/services/secret-storage/SecretStore.ts": "d26490b993e0e19e1f4b22170f4e5929ccceed7760509f322478159ac297df13",
    "src/services/secret-storage/encryptedStore.ts": "b9f628d273e8220680bf987abce85bbb37988165789106b5f6706e9c2eb3da4e",
    "src/services/monobank/MonobankTokenService.ts": "32060824d5dc2af52692017ce0a0c7f2b9d40f86e32377477433cbd2d3f7fa5b",
    "src/services/secret-storage/CLAUDE.md": "ee1d0754ce22157000b16c325b4a28524eb58266ffa5a962b46383eef49161bf",
    "src/services/monobank/CLAUDE.md": "281e23221e353bfb1d3010499baff32943051a032f6a97157408ba005088ae78"
  },
  "impl_created": [],
  "suite_at_freeze": {
    "useTransactionsViewModel filtering filters to a single category id — 3 of cat-a::useTransactionsViewModel filtering filters to a single category id — 3 of cat-a": "pass",
    "useTransactionsViewModel filtering filters to a single month prefix — 4 of 2026-08::useTransactionsViewModel filtering filters to a single month prefix — 4 of 2026-08": "pass",
    "useTransactionsViewModel filtering ANDs category and month — only 2 match both::useTransactionsViewModel filtering ANDs category and month — only 2 match both": "pass",
    "useTransactionsViewModel filtering shows the full list when no filter payload is present::useTransactionsViewModel filtering shows the full list when no filter payload is present": "pass",
    "useTransactionsViewModel filtering matches a month by string prefix ignoring timezone offset::useTransactionsViewModel filtering matches a month by string prefix ignoring timezone offset": "pass",
    "useTransactionsViewModel chip dismissal keeps the month filter when the category chip is dismissed::useTransactionsViewModel chip dismissal keeps the month filter when the category chip is dismissed": "pass",
    "useTransactionsViewModel chip dismissal keeps the category filter when the month chip is dismissed::useTransactionsViewModel chip dismissal keeps the category filter when the month chip is dismissed": "pass",
    "useTransactionsViewModel chip dismissal restores the full list when the only active filter is dismissed::useTransactionsViewModel chip dismissal restores the full list when the only active filter is dismissed": "pass",
    "useTransactionsViewModel focus re-application clears the active filter when re-focused without a payload::useTransactionsViewModel focus re-application clears the active filter when re-focused without a payload": "pass",
    "useTransactionsViewModel focus re-application replaces the active filter when navigated to with a new payload::useTransactionsViewModel focus re-application replaces the active filter when navigated to with a new payload": "pass",
    "CardsRepository reads returns an empty list for a user with no cards::CardsRepository reads returns an empty list for a user with no cards": "pass",
    "CardsRepository reads returns the cards belonging to one user, in sort order::CardsRepository reads returns the cards belonging to one user, in sort order": "pass",
    "CardsRepository reads finds a card by id::CardsRepository reads finds a card by id": "pass",
    "CardsRepository reads returns null when the id does not exist::CardsRepository reads returns null when the id does not exist": "pass",
    "CardsRepository reads returns null when no card carries the monobank account id::CardsRepository reads returns null when no card carries the monobank account id": "pass",
    "CardsRepository writes creates a card with the given fields and appends it to the order::CardsRepository writes creates a card with the given fields and appends it to the order": "pass",
    "CardsRepository writes updates only the fields it is given::CardsRepository writes updates only the fields it is given": "pass",
    "CardsRepository writes deletes a card so it can no longer be found::CardsRepository writes deletes a card so it can no longer be found": "pass",
    "CardsRepository writes rewrites sort order to match the given id sequence::CardsRepository writes rewrites sort order to match the given id sequence": "pass",
    "CardsRepository monobank cards creates a monobank card from an account and finds it by account id::CardsRepository monobank cards creates a monobank card from an account and finds it by account id": "pass",
    "CardsRepository monobank cards updates the existing card instead of adding a second one for the same account::CardsRepository monobank cards updates the existing card instead of adding a second one for the same account": "pass",
    "CardsRepository monobank cards returns only the cards linked to a monobank account::CardsRepository monobank cards returns only the cards linked to a monobank account": "pass",
    "CardsRepository test-database isolation writes a card that the next case must not see::CardsRepository test-database isolation writes a card that the next case must not see": "pass",
    "CardsRepository test-database isolation starts from an empty table despite the previous case writing one::CardsRepository test-database isolation starts from an empty table despite the previous case writing one": "pass",
    "resetSandboxEnvironment wipes the database AC-007 — leaves no transaction row behind::resetSandboxEnvironment wipes the database AC-007 — leaves no transaction row behind": "pass",
    "resetSandboxEnvironment wipes the database AC-008 — leaves no card row behind::resetSandboxEnvironment wipes the database AC-008 — leaves no card row behind": "pass",
    "resetSandboxEnvironment clears the key-value storage AC-009 — leaves no Monobank token behind::resetSandboxEnvironment clears the key-value storage AC-009 — leaves no Monobank token behind": "pass",
    "resetSandboxEnvironment clears the key-value storage AC-010 — leaves no account selection behind::resetSandboxEnvironment clears the key-value storage AC-010 — leaves no account selection behind": "pass",
    "resetSandboxEnvironment run twice AC-012 — leaves no user row behind on a second run::resetSandboxEnvironment run twice AC-012 — leaves no user row behind on a second run": "pass",
    "resetSandboxEnvironment outside a sandbox build AC-013 — raises SandboxOnlyError in a normal build::resetSandboxEnvironment outside a sandbox build AC-013 — raises SandboxOnlyError in a normal build": "pass",
    "useMonobankStore.connect in a sandbox build AC-003 — reports the production 401 text for a token that is not a test user::useMonobankStore.connect in a sandbox build AC-003 — reports the production 401 text for a token that is not a test user": "pass",
    "useMonobankStore.connect in a sandbox build AC-004 — connects on a test token surrounded by whitespace::useMonobankStore.connect in a sandbox build AC-004 — connects on a test token surrounded by whitespace": "pass",
    "useMonobankStore.connect in a sandbox build AC-005 — makes no fetch call at all, connect and its sync together::useMonobankStore.connect in a sandbox build AC-005 — makes no fetch call at all, connect and its sync together": "pass",
    "useMonobankStore.connect in a sandbox build AC-006 — creates one Monobank card in an empty database::useMonobankStore.connect in a sandbox build AC-006 — creates one Monobank card in an empty database": "pass",
    "useMonobankStore.connect in a sandbox build AC-007 — writes every fixture transaction through the production sync::useMonobankStore.connect in a sandbox build AC-007 — writes every fixture transaction through the production sync": "pass",
    "useMonobankStore.connect in a sandbox build AC-015 — refills the database after a sandbox reset empties it::useMonobankStore.connect in a sandbox build AC-015 — refills the database after a sandbox reset empties it": "pass",
    "cryptoKey.generateKey resolves its random source at call time AC-010 — draws from the source installed after the module was loaded::cryptoKey.generateKey resolves its random source at call time AC-010 — draws from the source installed after the module was loaded": "pass",
    "cryptoKey.generateKey resolves its random source at call time AC-011 — throws when the source is gone by the time it is called::cryptoKey.generateKey resolves its random source at call time AC-011 — throws when the source is gone by the time it is called": "pass",
    "index.js AC-012 — imports the random-values polyfill before anything else::index.js AC-012 — imports the random-values polyfill before anything else": "pass",
    "src/services/secret-storage/CLAUDE.md AC-015 — records where the random bytes come from on device::src/services/secret-storage/CLAUDE.md AC-015 — records where the random bytes come from on device": "pass",
    "package.json AC-016 — declares the polyfill as a runtime dependency::package.json AC-016 — declares the polyfill as a runtime dependency": "pass",
    "checkTestConvention CLI exits 0 when every tracked test is in the allowed form (AC-011)::checkTestConvention CLI exits 0 when every tracked test is in the allowed form (AC-011)": "pass",
    "checkTestConvention CLI exits 1 when a tracked test breaks the convention (AC-012)::checkTestConvention CLI exits 1 when a tracked test breaks the convention (AC-012)": "pass",
    "checkTestConvention CLI prints every violating path, not only the first (AC-013)::checkTestConvention CLI prints every violating path, not only the first (AC-013)": "pass",
    "checkTestConvention CLI exits 1 when the tree holds no test file at all (AC-014)::checkTestConvention CLI exits 1 when the tree holds no test file at all (AC-014)": "pass",
    "checkTestConvention CLI exits 1 when git is unavailable (AC-015)::checkTestConvention CLI exits 1 when git is unavailable (AC-015)": "pass",
    "findTestConventionViolations — the allowed form accepts a .test.ts nested under src/ (AC-001)::findTestConventionViolations — the allowed form accepts a .test.ts nested under src/ (AC-001)": "pass",
    "findTestConventionViolations — the allowed form accepts App.test.tsx at the repository root (AC-002)::findTestConventionViolations — the allowed form accepts App.test.tsx at the repository root (AC-002)": "pass",
    "findTestConventionViolations — the allowed form accepts a .test.ts nested under scripts/ (AC-003)::findTestConventionViolations — the allowed form accepts a .test.ts nested under scripts/ (AC-003)": "pass",
    "findTestConventionViolations — the allowed form ignores a non-test source file (AC-010)::findTestConventionViolations — the allowed form ignores a non-test source file (AC-010)": "pass",
    "findTestConventionViolations — the violating forms flags both .spec. and .tests. infixes (AC-004)::findTestConventionViolations — the violating forms flags both .spec. and .tests. infixes (AC-004)": "pass",
    "findTestConventionViolations — the violating forms flags a .test.js (AC-005)::findTestConventionViolations — the violating forms flags a .test.js (AC-005)": "pass",
    "findTestConventionViolations — the violating forms flags a .test.jsx (AC-006)::findTestConventionViolations — the violating forms flags a .test.jsx (AC-006)": "pass",
    "findTestConventionViolations — the violating forms flags a correctly named test inside a __tests__ directory (AC-007)::findTestConventionViolations — the violating forms flags a correctly named test inside a __tests__ directory (AC-007)": "pass",
    "findTestConventionViolations — the violating forms flags any file inside a __mocks__ directory (AC-008)::findTestConventionViolations — the violating forms flags any file inside a __mocks__ directory (AC-008)": "pass",
    "findTestConventionViolations — the violating forms flags an allowed-name test outside every allowed location (AC-009)::findTestConventionViolations — the violating forms flags an allowed-name test outside every allowed location (AC-009)": "pass",
    "useTransactionsStore.syncFromMonobank run a second time AC-013 — updates the existing rows instead of adding a second copy::useTransactionsStore.syncFromMonobank run a second time AC-013 — updates the existing rows instead of adding a second copy": "pass",
    "useTransactionsStore.syncFromMonobank run a second time AC-014 — settles back to idle rather than to an error::useTransactionsStore.syncFromMonobank run a second time AC-014 — settles back to idle rather than to an error": "pass",
    "SandboxMonobankService token resolution AC-002 — raises the production UNAUTHORIZED error for a token that is not a test user::SandboxMonobankService token resolution AC-002 — raises the production UNAUTHORIZED error for a token that is not a test user": "pass",
    "SandboxMonobankService statements AC-008 — returns the whole fixture set, ignoring the requested range::SandboxMonobankService statements AC-008 — returns the whole fixture set, ignoring the requested range": "pass",
    "SandboxMonobankService statements AC-009 — dates 4 of them on the day of the call::SandboxMonobankService statements AC-009 — dates 4 of them on the day of the call": "pass",
    "SandboxMonobankService statements AC-010 — dates none of them after the call moment::SandboxMonobankService statements AC-010 — dates none of them after the call moment": "pass",
    "SandboxMonobankService statements AC-011 — spreads them across 3 distinct calendar months::SandboxMonobankService statements AC-011 — spreads them across 3 distinct calendar months": "pass",
    "SandboxMonobankService statements AC-012 — carries a donations MCC on 4 of them::SandboxMonobankService statements AC-012 — carries a donations MCC on 4 of them": "pass",
    "transactionsToCsv writes a header row followed by one row per transaction::transactionsToCsv writes a header row followed by one row per transaction": "pass",
    "transactionsToCsv quotes and escapes fields containing commas, quotes, or newlines::transactionsToCsv quotes and escapes fields containing commas, quotes, or newlines": "pass",
    "transactionsToCsv renders an empty comment as an empty field::transactionsToCsv renders an empty comment as an empty field": "pass",
    "transactionsToJson produces parseable JSON of the transactions::transactionsToJson produces parseable JSON of the transactions": "pass",
    "MonobankAccountSelectionService returns null when the user has not chosen yet (= sync all)::MonobankAccountSelectionService returns null when the user has not chosen yet (= sync all)": "pass",
    "MonobankAccountSelectionService round-trips an explicit selection::MonobankAccountSelectionService round-trips an explicit selection": "pass",
    "MonobankAccountSelectionService persists an empty selection as an empty list (not null)::MonobankAccountSelectionService persists an empty selection as an empty list (not null)": "pass",
    "MonobankAccountSelectionService clears the selection back to null::MonobankAccountSelectionService clears the selection back to null": "pass",
    "MonobankAccountSelectionService treats malformed stored data as no selection::MonobankAccountSelectionService treats malformed stored data as no selection": "pass",
    "isSandboxBuild AC-001 — reports a normal build when react-native-config exposes no OPES_ENV key::isSandboxBuild AC-001 — reports a normal build when react-native-config exposes no OPES_ENV key": "pass",
    "formatMoney formats UAH with uk-UA grouping and the ₴ glyph::formatMoney formats UAH with uk-UA grouping and the ₴ glyph": "pass",
    "formatMoney defaults to UAH (₴) when no currency is set::formatMoney defaults to UAH (₴) when no currency is set": "pass",
    "formatMoney maps known numeric codes to glyphs::formatMoney maps known numeric codes to glyphs": "pass",
    "formatMoney renders negatives with a leading minus::formatMoney renders negatives with a leading minus": "pass",
    "formatMoney prefixes a + for non-negative amounts when showSign is set::formatMoney prefixes a + for non-negative amounts when showSign is set": "pass",
    "formatMoney falls back to the stored alpha symbol for codes without a glyph::formatMoney falls back to the stored alpha symbol for codes without a glyph": "pass",
    "formatMoney treats amounts as major units (does not divide by 100)::formatMoney treats amounts as major units (does not divide by 100)": "pass",
    "computeDonationStats returns zeroes for an empty list::computeDonationStats returns zeroes for an empty list": "pass",
    "computeDonationStats sums magnitudes and averages over donations (outflows are negative)::computeDonationStats sums magnitudes and averages over donations (outflows are negative)": "pass",
    "getMonobankService AC-001 — hands back a real MonobankService in a non-sandbox build::getMonobankService AC-001 — hands back a real MonobankService in a non-sandbox build": "pass",
    "createSqliteAdapter AC-002 — hands the adapter constructor the database name opes in a normal build::createSqliteAdapter AC-002 — hands the adapter constructor the database name opes in a normal build": "pass",
    "createSqliteAdapter AC-003 — hands the adapter constructor the database name opes_sandbox in a sandbox build::createSqliteAdapter AC-003 — hands the adapter constructor the database name opes_sandbox in a sandbox build": "pass",
    "showGeneralErrorBottomSheet AC-001 — titles the sheet \"Something went wrong\"::showGeneralErrorBottomSheet AC-001 — titles the sheet \"Something went wrong\"": "pass",
    "showGeneralErrorBottomSheet AC-002 — raises it on the existing error variant::showGeneralErrorBottomSheet AC-002 — raises it on the existing error variant": "pass",
    "SettingsScreen sandbox reset row AC-014 — renders the reset item in a sandbox build::SettingsScreen sandbox reset row AC-014 — renders the reset item in a sandbox build": "pass",
    "SettingsScreen sandbox reset row AC-015 — renders no reset item in a normal build::SettingsScreen sandbox reset row AC-015 — renders no reset item in a normal build": "pass",
    "SettingsScreen sandbox reset navigation AC-011 — sends the app back to Welcome once the reset item is pressed::SettingsScreen sandbox reset navigation AC-011 — sends the app back to Welcome once the reset item is pressed": "pass",
    "TransactionsScreen filter chips renders the category name in the category chip::TransactionsScreen filter chips renders the category name in the category chip": "pass",
    "TransactionsScreen filter chips renders the four-digit year in the month chip::TransactionsScreen filter chips renders the four-digit year in the month chip": "pass",
    "TransactionsScreen filter chips places the category chip first in the chip row::TransactionsScreen filter chips places the category chip first in the chip row": "pass",
    "TransactionsScreen filtered empty state shows the filtered empty-state copy when a filter matches nothing::TransactionsScreen filtered empty state shows the filtered empty-state copy when a filter matches nothing": "pass",
    "TransactionsScreen filtered empty state keeps the category chip visible while the filtered list is empty::TransactionsScreen filtered empty state keeps the category chip visible while the filtered list is empty": "pass",
    "createCardsStore loads cards for a user::createCardsStore loads cards for a user": "pass",
    "createCardsStore appends a card to the list::createCardsStore appends a card to the list": "pass",
    "createCardsStore rolls back card order on reorderCards failure::createCardsStore rolls back card order on reorderCards failure": "pass",
    "createCardsStore optimistically updates a card and reconciles with the repository result::createCardsStore optimistically updates a card and reconciles with the repository result": "pass",
    "createCardsStore rolls back the card on updateCard failure::createCardsStore rolls back the card on updateCard failure": "pass",
    "createCardsStore optimistically removes a card on deleteCard::createCardsStore optimistically removes a card on deleteCard": "pass",
    "createCardsStore rolls back the removed card on deleteCard failure::createCardsStore rolls back the removed card on deleteCard failure": "pass",
    "createCreateCardStore creates a card with form values and resets the store::createCreateCardStore creates a card with form values and resets the store": "pass",
    "createCreateCardStore passes the selected image URI to card creation::createCreateCardStore passes the selected image URI to card creation": "pass",
    "createCreateCardStore returns null and sets error on repository failure::createCreateCardStore returns null and sets error on repository failure": "pass",
    "HomeScreen reads cards on focus AC-001 — reads the cards once when the screen mounts and is focused::HomeScreen reads cards on focus AC-001 — reads the cards once when the screen mounts and is focused": "pass",
    "HomeScreen reads cards on focus AC-002 — reads the cards again when the screen is focused a second time::HomeScreen reads cards on focus AC-002 — reads the cards again when the screen is focused a second time": "pass",
    "HomeScreen reads cards on focus AC-003 — renders the card the second read returns::HomeScreen reads cards on focus AC-003 — renders the card the second read returns": "pass",
    "HomeScreen does not re-read cards on Monobank activity AC-004 — does not re-read when the Monobank status turns connected::HomeScreen does not re-read cards on Monobank activity AC-004 — does not re-read when the Monobank status turns connected": "pass",
    "HomeScreen does not re-read cards on Monobank activity AC-005 — reads once when a saved token connects during mount::HomeScreen does not re-read cards on Monobank activity AC-005 — reads once when a saved token connects during mount": "pass",
    "HomeScreen does not re-read cards on Monobank activity AC-006 — does not re-read when pull-to-refresh syncs::HomeScreen does not re-read cards on Monobank activity AC-006 — does not re-read when pull-to-refresh syncs": "pass",
    "HomeScreen does not re-read cards on Monobank activity AC-007 — does not re-read when returning to the foreground syncs::HomeScreen does not re-read cards on Monobank activity AC-007 — does not re-read when returning to the foreground syncs": "pass",
    "useMonobankStore.disconnect when clearing the token rejects AC-003 — leaves the status at connected::useMonobankStore.disconnect when clearing the token rejects AC-003 — leaves the status at connected": "pass",
    "useMonobankStore.disconnect when clearing the token rejects AC-004 — keeps the client name::useMonobankStore.disconnect when clearing the token rejects AC-004 — keeps the client name": "pass",
    "useMonobankStore.disconnect when clearing the token rejects AC-005 — keeps the error message that was already there::useMonobankStore.disconnect when clearing the token rejects AC-005 — keeps the error message that was already there": "pass",
    "useMonobankStore.disconnect when clearing the token rejects AC-006 — keeps the linked accounts::useMonobankStore.disconnect when clearing the token rejects AC-006 — keeps the linked accounts": "pass",
    "useMonobankStore.disconnect when clearing the token rejects AC-007 — keeps the account selection::useMonobankStore.disconnect when clearing the token rejects AC-007 — keeps the account selection": "pass",
    "useMonobankStore.disconnect when clearing the token rejects AC-008 — does not clear the cached Monobank service::useMonobankStore.disconnect when clearing the token rejects AC-008 — does not clear the cached Monobank service": "pass",
    "useMonobankStore.disconnect when clearing the token rejects AC-009 — does not clear the account selection::useMonobankStore.disconnect when clearing the token rejects AC-009 — does not clear the account selection": "pass",
    "useMonobankStore.disconnect when clearing the token rejects AC-010 — does not reset the transactions store::useMonobankStore.disconnect when clearing the token rejects AC-010 — does not reset the transactions store": "pass",
    "useMonobankStore.disconnect when clearing the token rejects AC-011 — raises the general error sheet::useMonobankStore.disconnect when clearing the token rejects AC-011 — raises the general error sheet": "pass",
    "useMonobankStore.disconnect when clearing the token rejects AC-012 — resolves rather than rejecting::useMonobankStore.disconnect when clearing the token rejects AC-012 — resolves rather than rejecting": "pass",
    "useMonobankStore.disconnect when clearing the token resolves AC-013 — moves the status to idle::useMonobankStore.disconnect when clearing the token resolves AC-013 — moves the status to idle": "pass",
    "useMonobankStore.disconnect when clearing the token resolves AC-014 — clears the cached Monobank service::useMonobankStore.disconnect when clearing the token resolves AC-014 — clears the cached Monobank service": "pass",
    "useMonobankStore.disconnect when clearing the token resolves AC-015 — clears the account selection::useMonobankStore.disconnect when clearing the token resolves AC-015 — clears the account selection": "pass",
    "useMonobankStore.disconnect when clearing the token resolves AC-016 — resets the transactions store::useMonobankStore.disconnect when clearing the token resolves AC-016 — resets the transactions store": "pass",
    "useMonobankStore.connect when saving the token rejects AC-017 — raises the general error sheet, not \"Connection Failed\"::useMonobankStore.connect when saving the token rejects AC-017 — raises the general error sheet, not \"Connection Failed\"": "pass",
    "useMonobankStore.connect when saving the token rejects AC-020 — puts the store in the error status::useMonobankStore.connect when saving the token rejects AC-020 — puts the store in the error status": "pass",
    "useMonobankStore.connect when the Monobank API rejects AC-018 — keeps \"Connection Failed\" for a MonobankError::useMonobankStore.connect when the Monobank API rejects AC-018 — keeps \"Connection Failed\" for a MonobankError": "pass",
    "useMonobankStore.connect when the Monobank API rejects AC-019 — keeps \"Connection Failed\" for an error that is not a MonobankError::useMonobankStore.connect when the Monobank API rejects AC-019 — keeps \"Connection Failed\" for an error that is not a MonobankError": "pass",
    " renders correctly:: renders correctly": "pass"
  }
}

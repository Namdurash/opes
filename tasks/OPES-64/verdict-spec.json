{ "schema": 1,
  "gate": "spec-judge",
  "subject": "spec.md",
  "subject_sha256": "5879aed97b7ae55f1923be38d11b32544517bfd2a4fe229604bf7179c07e489e",
  "judge_agent": "aif-spec-judge",
  "at": "2026-09-16T12:00:00Z",
  "pass": true,
  "checked": [
    "The meta `surfaces` list against the ticket's 'Surfaces the change is seen through' section — all five ticket surfaces (useMonobankStore connect/disconnect/loadSavedToken, types.ts, ConnectMonobankScreen.tsx, HomeScreen.tsx, useTransactionsStore.syncFromMonobank) have at least one criterion",
    "AC-001 and AC-006 against the ticket's 'Save still pending' / 'Clear still pending' edge cases and the Risk section's demand that ordering be pinned as an observation of the intermediate state — both assert status while the token-service promise is unsettled, not the final state",
    "AC-002, AC-003, AC-004, AC-005 against the ticket's 'A failed save is a failed connection' paragraph — status, message type, absence of rejection and bottom-sheet count are four separate single-value assertions, none bundled",
    "AC-007 through AC-014 against the ticket's 'A failed clear is surfaced actively, and nothing is torn down' bullets — each of the three named skipped side effects (clearMonobankService, account-selection clear, transactions reset) gets its own criterion so a failure is attributable",
    "AC-012 against the ticket's explicit `showErrorBottomSheet` requirement and against the 'Deliberately left open' section, which mandates the sheet for the disconnect failure but not its wording",
    "AC-015 and AC-016 against the 'Clear resolves' edge case",
    "AC-017 and AC-018 against `Promise<string | null>` in the ticket's 'Saved-token restoration is async end to end' section — both resolve branches covered",
    "AC-019 and AC-020 against the ticket's two types.ts signature requirements (loadSavedToken and disconnect); each pins a distinct type-level fixture, with tsc exit code as the observable",
    "AC-021 through AC-024 against the ticket's ConnectMonobankScreen restore-effect bullet — restored token, null branch, rejection branch and the 'not surfaced to the user' silence are separate criteria",
    "AC-025 against the ticket's requirement that the disconnect call site in ConnectMonobankScreen be updated",
    "AC-026 against the ticket's 'fire-and-forget with a `.catch` ... produces no unhandled rejection' bullet — asserted as a count of recorded global rejection events, which a test can observe",
    "AC-027 and AC-028 against the syncFromMonobank bullet, including the null branch the ticket does not restate",
    "Every `expect` field across AC-001..AC-028 for whether it is a value a test could compare against — all are a literal string, a count, or an exit code; none is a judgement word or empty",
    "Every `from` field against the ticket text, character for character, for the twenty criteria that quote the ticket rather than an AS id — all are verbatim substrings of tasks/OPES-64/ticket.md",
    "AS-001 through AS-010 for content that is really a limit of verification rather than a statement of behaviour — each states a behavioural decision plus the alternative rejected; AS-004's note that the criteria pin the message type and not its text has its verification counterpart correctly filed as VG-005, not left in assumptions",
    "VG-001 through VG-007 for the converse — each describes what the cycle will not establish (doubled token service, no on-disk observation, no concurrency, no error copy, no post-failure screen state), none states a behavioural decision",
    "Criteria against the ticket's 'Deliberately left open' section to confirm no criterion freezes the error-message wording the ticket refused to decide",
    "The connect bottom-sheet criterion AC-005, which the ticket does not mandate, against AS-003, which records that extension as an assumption rather than presenting it as a ticket requirement"
  ],
  "findings": [] }

# OPES-64 — how this was arrived at

Generated by `aif explain`. Every line below was written by a station
while it decided and is checked by that station gate — nothing here is
narrated after the fact. Regenerate it; do not edit it.

## Specification

```mermaid
flowchart LR
  T0("**Clear rejects** — `status: 'error'`, side effects skipped, error bottom sheet shown")
  T1("**Clear resolves** — status becomes `idle` and the side effects run.")
  T2("**Clear still pending** — status is still `connected`.")
  T3("**Save rejects** — `status: 'error'`")
  T4("**Save still pending** — status is `connecting`, never `connected`.")
  T5("**`loadSavedToken` rejects** — the field is left empty, no bottom sheet")
  T6("**`loadSavedToken` resolves `null`** — the Connect screen field is left empty, no error sh…")
  T7("The `ConnectMonobankScreen.tsx` effect awaits it and then `setValue`s the restored token.")
  T8("The `HomeScreen.tsx` startup effect calls it fire-and-forget with a `.catch`")
  T9("`clearMonobankService`, the account-selection")
  T10("`monobankTokenService.get ` call and passes the resolved token onward.")
  T11("`src/features/monobank/types.ts` and its call site in `ConnectMonobankScreen.tsx` are upda…")
  T12("`useMonobankStore.loadSavedToken` becomes `async` returning `Promise string null `")
  T13("signature in `src/features/monobank/types.ts` is updated to match.")
  T14("status becomes `idle` and the side effects run")
  T15("the failure is **also raised through the existing error bottom sheet**")
  T16("the follow-on side effects are **skipped**: `clearMonobankService`")
  T17("the transactions reset do not run, because the secret is still stored")
  T18("this restoration path is the one place a failure is **not** surfaced to the user.")
  AC_001["AC-001 — the store's status equals `connecting` → `connecting`"]
  AC_002["AC-002 — the store's status equals `error` → `error`"]
  AC_003["AC-003 — the type of the store's `errorMessage` equals `string` → `string`"]
  AC_004["AC-004 — the rejection count recorded for the awaited `connect` call … → 0"]
  AC_005["AC-005 — the `showErrorBottomSheet` call count equals 1 → 1"]
  AC_006["AC-006 — the store's status equals `connected` → `connected`"]
  AC_007["AC-007 — the store's status equals `error` → `error`"]
  AC_008["AC-008 — the type of the store's `errorMessage` equals `string` → `string`"]
  AC_009["AC-009 — the `clearMonobankService` call count equals 0 → 0"]
  AC_010["AC-010 — the `monobankAccountSelectionService.clear` call count equal… → 0"]
  AC_011["AC-011 — the `useTransactionsStore.reset` call count equals 0 → 0"]
  AC_012["AC-012 — the `showErrorBottomSheet` call count equals 1 → 1"]
  AC_013["AC-013 — the rejection count recorded for the awaited `disconnect` ca… → 0"]
  AC_014["AC-014 — the store's `clientName` equals `Ada` → `Ada`"]
  AC_015["AC-015 — the store's status equals `idle` → `idle`"]
  AC_016["AC-016 — the `clearMonobankService` call count equals 1 → 1"]
  AC_017["AC-017 — the awaited value is null → `null`"]
  AC_018["AC-018 — the awaited value equals `saved-token` → `saved-token`"]
  AC_019["AC-019 — the type-check process exit code equals 0 → 0"]
  AC_020["AC-020 — the type-check process exit code equals 0 → 0"]
  AC_021["AC-021 — the token input's `value` prop equals `saved-token` → `saved-token`"]
  AC_022["AC-022 — the length of the token input's `value` prop equals 0 → 0"]
  AC_023["AC-023 — the length of the token input's `value` prop equals 0 → 0"]
  AC_024["AC-024 — the `showErrorBottomSheet` call count equals 0 → 0"]
  AC_025["AC-025 — the store's `disconnect` call count equals 1 → 1"]
  AC_026["AC-026 — the count of rejection events recorded by the global listene… → 0"]
  AC_027["AC-027 — the first argument of the `getMonobankService` call equals `… → `saved-token`"]
  AC_028["AC-028 — the `getMonobankService` call count equals 0 → 0"]
  AS_001{{"AS-001 — connect absorbs a rejecting save: its own promise resolves, and the fa…"}}
  AS_002{{"AS-002 — disconnect likewise absorbs a rejecting clear and its own promise reso…"}}
  AS_003{{"AS-003 — A rejecting save travels through connect's existing catch branch, so a…"}}
  AS_004{{"AS-004 — quot;with a message quot; means errorMessage holds a string on both fa…"}}
  AS_005{{"AS-005 — On a rejecting clear the store keeps clientName, accounts and selected…"}}
  AS_006{{"AS-006 — A rejecting loadSavedToken on ConnectMonobankScreen also sets no field…"}}
  AS_007{{"AS-007 — syncFromMonobank with no saved token returns without moving syncStatus…"}}
  AS_008{{"AS-008 — loadSavedToken resolving null leaves the store's status untouched rath…"}}
  AS_009{{"AS-009 — No new MonobankConnectionStatus member is introduced: idle, connecting…"}}
  AS_010{{"AS-010 — A connect whose save resolves but whose subsequent card upsert rejects…"}}
  VG_001[/"VG-001 — Every criterion runs against a doubled token service; nothing in this …"/]
  VG_002[/"VG-002 — A resolved clear is observed as a call that resolved, not as an absenc…"/]
  VG_003[/"VG-003 — The pending-window observations are made with manually settled promise…"/]
  VG_004[/"VG-004 — Concurrency between these actions is not exercised: overlapping connec…"/]
  VG_005[/"VG-005 — The text the user actually reads on either failure is not established …"/]
  VG_006[/"VG-006 — What ConnectMonobankScreen renders once the status is error after a fa…"/]
  VG_007[/"VG-007 — No criterion observes the token service from the outside during the pe…"/]
  T4 --> AC_001
  T3 --> AC_002
  T2 --> AC_006
  T0 --> AC_007
  T16 --> AC_009
  T9 --> AC_010
  T17 --> AC_011
  T15 --> AC_012
  T1 --> AC_015
  T14 --> AC_016
  T12 --> AC_017
  T12 --> AC_018
  T13 --> AC_019
  T11 --> AC_020
  T7 --> AC_021
  T6 --> AC_022
  T5 --> AC_023
  T18 --> AC_024
  T8 --> AC_026
  T10 --> AC_027
  AS_004 -->|"exists only because of this"| AC_003
  AS_001 -->|"exists only because of this"| AC_004
  AS_003 -->|"exists only because of this"| AC_005
  AS_004 -->|"exists only because of this"| AC_008
  AS_002 -->|"exists only because of this"| AC_013
  AS_005 -->|"exists only because of this"| AC_014
  AS_002 -->|"exists only because of this"| AC_025
  AS_007 -->|"exists only because of this"| AC_028
  AS_009 -.->|"rests on"| AC_006
  AS_009 -.->|"rests on"| AC_007
  VG_001 -.->|"leaves unproven"| AC_001
  VG_001 -.->|"leaves unproven"| AC_006
  VG_001 -.->|"leaves unproven"| AC_015
  VG_001 -.->|"leaves unproven"| AC_016
  VG_002 -.->|"leaves unproven"| AC_015
  VG_002 -.->|"leaves unproven"| AC_016
  VG_003 -.->|"leaves unproven"| AC_001
  VG_003 -.->|"leaves unproven"| AC_006
  VG_005 -.->|"leaves unproven"| AC_003
  VG_005 -.->|"leaves unproven"| AC_005
  VG_005 -.->|"leaves unproven"| AC_008
  VG_005 -.->|"leaves unproven"| AC_012
  VG_006 -.->|"leaves unproven"| AC_007
  VG_007 -.->|"leaves unproven"| AC_001
  VG_007 -.->|"leaves unproven"| AC_006
```

### Assumptions — the decisions the ticket did not make

**AS-001 — connect absorbs a rejecting save: its own promise resolves, and the failure is presented only through store state and the bottom sheet, never by rejecting to the caller.**

- the ticket left it open: the ticket says the failure is surfaced as a connection failure rather than an unhandled promise rejection, but does not say whether connect's own promise settles as resolved or as rejected-and-handled by each caller.
- instead of: letting connect re-throw after setting the status, which would oblige every call site — the screen's handleSubmit callback included — to attach its own handler.
- carries: AC-004

**AS-002 — disconnect likewise absorbs a rejecting clear and its own promise resolves, so ConnectMonobankScreen keeps handing the action straight to onPress with no wrapper.**

- the ticket left it open: the ticket requires the disconnect call site in ConnectMonobankScreen to be updated accordingly without saying whether that update is an await-with-catch wrapper or an unchanged pass-through.
- instead of: having disconnect reject and wrapping the Disconnect button's onPress in a screen-level try/catch that would own the error presentation.
- carries: AC-013, AC-025

**AS-003 — A rejecting save travels through connect's existing catch branch, so a failed write raises the same Connection Failed bottom sheet a network failure already raises.**

- the ticket left it open: the ticket mandates the bottom sheet for the disconnect failure and is silent on whether a failed save reaches one at all.
- instead of: giving the save branch its own catch and its own presentation, distinct from the failure path a bad token already takes.
- carries: AC-005

**AS-004 — "with a message" means errorMessage holds a string on both failure branches; the criteria pin its type, not its text.**

- the ticket left it open: the ticket deliberately leaves the exact wording of the error messages open while still requiring each failure branch to set a message.
- instead of: fixing an exact message string in the criteria, which would freeze copy the ticket explicitly refused to decide.
- carries: AC-003, AC-008

**AS-005 — On a rejecting clear the store keeps clientName, accounts and selectedAccountIds as they were — only status and errorMessage move — so the screen still shows the user as linked to that client.**

- the ticket left it open: the ticket says the tear-down side effects are skipped and the user is not presented as disconnected, but does not say what happens to the store's own connected-identity fields.
- instead of: clearing clientName and accounts while leaving status at error, which would present the user as half-disconnected while the token is still on disk.
- carries: AC-014

**AS-006 — A rejecting loadSavedToken on ConnectMonobankScreen also sets no field-level validation error and does not move the store's status.**

- the ticket left it open: the ticket says the rejection is caught and the field left empty rather than blocking the form with an error, without naming which of the screen's other error channels must stay silent.
- instead of: marking the token field invalid or dropping the store to idle, either of which would make a transient read failure look like user input trouble.
- carries: **nothing — no criterion depends on it, so nothing would fail if it were wrong**

**AS-007 — syncFromMonobank with no saved token returns without moving syncStatus and without constructing a Monobank service.**

- the ticket left it open: the ticket only says the get() call is awaited and the resolved token passed onward; it does not restate what the null branch does now that the read is asynchronous.
- instead of: treating a missing token as a sync failure that sets syncStatus to error and raises a sheet on every cold start before a connection exists.
- carries: AC-028

**AS-008 — loadSavedToken resolving null leaves the store's status untouched rather than forcing it to idle.**

- the ticket left it open: the ticket pins what the Connect screen does on null (field left empty) but says nothing about whether the store should react to the absence of a saved token.
- instead of: resetting status to idle on a null result, which would race a connect already in flight and could erase a connecting state.
- carries: **nothing — no criterion depends on it, so nothing would fail if it were wrong**

**AS-009 — No new MonobankConnectionStatus member is introduced: idle, connecting, connected and error carry every state named here, including the window in which a clear is pending.**

- the ticket left it open: the ticket requires an existing MonobankConnectionStatus value for the failure and names connected for the pending-clear window, but a disconnect that can now fail is the obvious place someone would add a disconnecting state.
- instead of: adding a disconnecting member, which would change every status comparison in ConnectMonobankScreen and HomeScreen.
- carries: AC-006, AC-007

**AS-010 — A connect whose save resolves but whose subsequent card upsert rejects stays a connection failure with status error, exactly as today; only the save branch is being pinned.**

- the ticket left it open: the ticket pins the save branch of connect and does not say whether the rest of the try block keeps its current failure behaviour once the save branch is given explicit semantics.
- instead of: treating a persisted token as a successful connection and letting a later upsert failure leave the store at connected with a partial card set.
- carries: **nothing — no criterion depends on it, so nothing would fail if it were wrong**

### What this cycle will not establish

- **VG-001** Every criterion runs against a doubled token service; nothing in this cycle exercises the real Keychain-backed SecretStore, so a green suite would not establish that a save reached the device or that a clear removed anything from it. (leaves unproven: AC-001, AC-006, AC-015, AC-016)
- **VG-002** A resolved clear is observed as a call that resolved, not as an absence of bytes on disk; the suite would not detect a residue of the kind OPES-63 found in the plaintext MMKV file. (leaves unproven: AC-015, AC-016)
- **VG-003** The pending-window observations are made with manually settled promises under Jest; real device latency, and what the Connect screen renders to the user during that window, are not exercised. (leaves unproven: AC-001, AC-006)
- **VG-004** Concurrency between these actions is not exercised: overlapping connect and disconnect calls, a second connect issued during a pending save, or a disconnect issued while a sync is in flight. (leaves unproven: no criterion in particular)
- **VG-005** The text the user actually reads on either failure is not established — only that a string is present and that the bottom sheet was invoked. (leaves unproven: AC-003, AC-005, AC-008, AC-012)
- **VG-006** What ConnectMonobankScreen renders once the status is error after a failed disconnect is not exercised, so nothing here establishes that the user can retry the disconnect or leave that state. (leaves unproven: AC-007)
- **VG-007** No criterion observes the token service from the outside during the pending window, so the suite establishes the order in which the store announces its status but not that the underlying write or delete had genuinely not yet happened. (leaves unproven: AC-001, AC-006)

---

Drawn from:

- `spec.md` sha256 `5879aed97b7ae55f1923be38d11b32544517bfd2a4fe229604bf7179c07e489e`

If those no longer match the files, this drawing is stale — run `aif explain OPES-64` again.

{ "schema": 1,
  "gate": "plan-judge",
  "subject": "plan.md",
  "subject_sha256": "8e2385e8028e42f90783f94ab2911a490d9ed22386cbdc3f01be8098790ca0fa",
  "judge_agent": "aif-plan-judge",
  "at": "2026-09-17T15:42:00Z",
  "checked": [
    "Read tasks/OPES-64/plan.md in full — files, decisions D-001..D-018, ac_coverage, surface_map, external.",
    "Read tasks/OPES-64/spec.md in full — all 20 acceptance criteria, AS-001..AS-004, VG-001..VG-005, non_goals.",
    "Read src/shared/ui/bottom-sheet/index.ts — showSuccessBottomSheet/showErrorBottomSheet/NotificationSheetConfig already there; the plan's showGeneralErrorBottomSheet snippet is a direct addition in the same pattern, nothing left to invent.",
    "Read src/shared/ui/CLAUDE.md — confirmed the current 'Errors:' bullet names only showErrorBottomSheet, matching the premise D-013 rewrites it from.",
    "Read src/shared/ui/index.ts — `export * from './bottom-sheet'` already there, so D-004's 'add nothing here' claim holds.",
    "Read src/features/monobank/state/useMonobankStore.ts in full — current connect()/disconnect() bodies match the plan's before/after diffs verbatim, including the exact fallback-message literal the plan hoists into CONNECT_FAILURE_MESSAGE.",
    "Read src/features/monobank/types.ts — MonobankConnectionStatus and the five state fields the plan says stay untouched are exactly as described.",
    "Grepped showErrorBottomSheet/showSuccessBottomSheet/showGeneralErrorBottomSheet repo-wide — no other call site needs updating for the new export.",
    "Read src/stores/useBottomSheetStore.ts — BottomSheetVariant already has 'error', so reusing it needs no edit to useBottomSheetStore or GlobalBottomSheet.",
    "Read src/features/monobank/state/useMonobankStore.test.ts (sandbox suite) — never calls disconnect(), six cases unaffected by this ticket, and it already mocks bottom-sheet/database the same way D-018 describes, supporting D-014's 'leave this file alone' claim.",
    "Read src/services/monobank/serviceInstance.ts — clearMonobankService is a plain named export with no injection seam, matching D-017's jest.mock rationale.",
    "Read src/services/monobank/MonobankTokenService.ts — save(token, clientName) and clear() signatures match the calls in useMonobankStore.ts exactly.",
    "Grepped 'disconnect' under src/features/monobank — only ConnectMonobankScreen.tsx calls it, via `onPress={disconnect}` with no wrapper, confirming AC-012/D-007's fulfil-not-reject requirement and that no second call site exists.",
    "Read scripts/checkTestConvention.ts — confirms tracked-path naming/placement enforcement the plan leans on for the two new *.test.ts files.",
    "Read src/features/monobank/ConnectMonobankScreen.tsx in full — errorMessage is never rendered, confirming D-011's claim; status/clientName/action functions are the only useMonobankStore fields it reads, none of which change shape.",
    "Read src/features/monobank/index.ts — feature barrel re-exports only ConnectMonobankScreen, useMonobankStore, and three type names, none touched by this plan.",
    "Grepped useMonobankStore usage repo-wide — src/features/home/HomeScreen.tsx only reads status, clientName and loadSavedToken, none altered here, so it needs no edit.",
    "Grepped BottomSheetVariant repo-wide — GlobalBottomSheet.tsx already maps the 'error' variant, so reusing it touches nothing there.",
    "Read src/services/monobank/MonobankAccountSelectionService.ts — clear() is synchronous void, matching the unawaited call disconnect() keeps outside the try.",
    "Read src/models/cards/CardsRepository.ts — `database` is read lazily inside each async method, never at construction, so `new CardsRepository()` at module scope cannot throw against D-018's `{ database: {} }` stub.",
    "Read src/services/database/database.ts and src/services/database/index.ts — the barrel does `export * from './database'`, so jest.mock of 'services/database/database' (not the barrel) is the correct, already-used interception point.",
    "Read src/features/transactions/state/useTransactionsStore.ts imports and reset() — synchronous, named, matches disconnect's unchanged call; TransactionsRepository is constructed at module scope with the same lazy-database pattern as CardsRepository.",
    "Read src/models/transactions/TransactionsRepository.ts — same lazy-database-access pattern, so importing useTransactionsStore under the database stub is safe too.",
    "Searched for a second monobank screen and a per-feature src/features/monobank/CLAUDE.md — neither exists, so the spec's 'two screens' and any monobank-specific doc are not forced edits.",
    "Compared ac_coverage against surface_map for all 20 criteria — AC-001 and AC-002 are the only pair where the sets differ (missing src/shared/ui/CLAUDE.md), matching exactly the two flags given; every other criterion's ac_coverage equals its surface's surface_map entry.",
    "Adjudicated AC-001/AC-002 — both `then` clauses assert only on useBottomSheetStore's runtime config from invoking the helper; nothing in either criterion touches documentation, and the plan's own Coverage section states the CLAUDE.md edit serves no criterion by design (a DoD-driven doc move, not an AC-driven behavior)."
  ],
  "guesses": [],
  "missing_files": [],
  "surface_adjudications": [
    { "ac": "AC-001",
      "verdict": "intended",
      "why": "AC-001 only asserts the bottom-sheet store's config.title after invoking the helper — a runtime check that has nothing to do with the CLAUDE.md prose the plan updates separately for the Definition of Done." },
    { "ac": "AC-002",
      "verdict": "intended",
      "why": "AC-002 only asserts the bottom-sheet store's config.variant after invoking the helper — the same runtime-only slice as AC-001, unrelated to the documentation file's content." }
  ] }

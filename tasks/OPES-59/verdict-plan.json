{ "schema": 1,
  "gate": "plan-judge",
  "subject": "plan.md",
  "subject_sha256": "f2bc80431439a5b9d82d4b381127a70b7b54ba56c9553ab5a86d21b81d9da49a",
  "judge_agent": "aif-plan-judge",
  "at": "2026-08-18T15:12:00Z",
  "checked": [
    "src/shared/env/env.ts and src/shared/env/index.ts — isSandboxBuild() is fully specified (D-001–D-003, condition Config.OPES_ENV === 'sandbox'); confirmed the barrel already does `export * from './env'` so no barrel edit is forced",
    "src/services/database/createSqliteAdapter.ts (new) — D-004/D-005/D-006 give the exact generic constructor type, the full options object (schema, migrations, jsi, dbName, onSetUpError), and the private resolver; cross-checked every key against the current inline object in database.ts, nothing left unaccounted for",
    "src/services/database/database.ts — D-007/D-008 reduce the device branch to a require + createSqliteAdapter(SQLiteAdapter) call and leave the jest/LokiJS branch untouched; read the current file in full to confirm exactly which lines move out",
    "src/services/database/index.ts — confirmed the barrel re-exports only database/schema/models; no consumer other than database.ts needs createSqliteAdapter, so it does not need adding to this barrel",
    "src/services/monobank/serviceInstance.ts and types.ts — D-009's guard (`new MonobankError('FORBIDDEN', ...)` before the cache is consulted) checked against MonobankError's real constructor signature and against MonobankErrorCode, which already includes 'FORBIDDEN'; MonobankError is exported from ./types, a same-folder import",
    "src/features/monobank/state/useMonobankStore.ts and src/features/transactions/state/useTransactionsStore.ts — read both in full; both call getMonobankService exclusively (no direct fetch, no `new MonobankService`), confirming D-010's 'change no line' is correct rather than an omission",
    "src/features/monobank/ConnectMonobankScreen.tsx — read in full; MONOBANK_TOKEN_URL is only passed to Linking.openURL, never fetched, so it does not bypass the getMonobankService guard",
    "src/features/home/HomeScreen.tsx and src/features/transactions/hooks/useAppForegroundSync.ts — traced every sync trigger AS-010 lists (Home pull-to-refresh, post-connect autosync, on-open autosync, silent foreground sync, Transactions pull-to-refresh) back to useTransactionsStore.syncFromMonobank, confirming the single choke point claim",
    "src/models/maintenance/DatabaseMaintenanceRepository.ts and index.ts (new) — D-011/D-012's `database.write(async () => { await database.unsafeResetDatabase() })` checked against test/db/createTestDatabase.ts's own teardown, which uses the identical pattern, and against WatermelonDB's Database/index.js which asserts unsafeResetDatabase() runs inside a writer",
    "src/models/cards/CardsRepository.ts, CardsRepository.test.ts, src/models/CLAUDE.md, src/models/users/UsersRepository.ts, src/models/userOverrides/UserOverridesRepository.ts, and the cards/users/index.ts barrels — read to confirm the repository-folder convention (contract interface + class + `export * from` barrel) that D-011/D-012 leave implicit",
    "src/services/sandbox/resetSandboxEnvironment.ts and index.ts (new) — D-013–D-016's SandboxOnlyError placement, call ordering, module-scope repository instantiation, and deep imports for the two key-value singletons checked against MonobankTokenService.ts / MonobankAccountSelectionService.ts, whose .clear() calls are idempotent (safe to call twice, per AS-013)",
    "src/features/settings/state/useSettingsStore.ts — read the current file in full; D-017–D-019 specify the resetSandbox action body precisely, except for what isResettingSandbox is actually consumed by",
    "src/features/settings/SettingsScreen.tsx and SettingsScreen.styles.ts — read in full; confirmed the `card` and `settingInfo` style keys D-021/D-022 reuse already exist, and that D-020/D-021/D-023's exact JSX for the reset row never reads isResettingSandbox",
    "src/app/navigation/routes.ts, types.ts, index.ts, RootNavigator.tsx, src/app/CLAUDE.md — confirmed ROOT_ROUTES.WELCOME and SettingsScreenNavigationProp (a NativeStackNavigationProp, which carries .reset()) already exist, so D-023 forces no navigation-layer edit",
    "src/stores/useUserStore.ts and src/features/welcome/WelcomeScreen.tsx — traced the post-reset flow: WelcomeScreen calls bootstrap() in a useEffect on every mount, re-deriving currentUserId/isCheckedIn from the wiped users table after navigation.reset remounts it, so useUserStore needs no edit despite never appearing in the plan",
    "src/features/cards/state/useCardsStore.ts and HomeScreen.tsx's loadCardsByUser effect — confirmed the same self-healing pattern for cards, so the in-memory cards cache needs no explicit reset either",
    "android/app/build.gradle — read in full; D-024–D-027's four edits checked line by line; confirmed android/app/src/main/res/values/strings.xml already defines app_name (so the sandbox-only resValue override doesn't strand the standard flavor) and that no per-flavor AndroidManifest.xml exists to conflict with the new dimension",
    "node_modules/@react-native-community/cli-platform-android's runAndroid/buildAndroid option arrays — confirmed --mode and --appIdSuffix are real flags in the installed 20.1.0 CLI, and that --appIdSuffix is joined without a leading dot, matching D-027's literal 'sandbox' argument and D-025's Gradle-side '.sandbox' suffix to the same final applicationId",
    ".gitignore — confirmed `.env` / `.env.*` / `!.env.example` are the actual ignore rules, validating D-029's claim that .env.sandbox is correctly excluded from the manifest",
    "test/setup.js, test/CLAUDE.md, test/db/createTestDatabase.ts, test/db/index.ts, jest.config.js — confirmed the existing `jest.mock('react-native-config', () => ({ default: {} }))` already hands tests a mutable Config object, so no shared-harness file needs editing for the isSandboxBuild() test seam the plan's prose describes",
    "scripts/checkTestConvention.ts — confirmed it derives test files from `git ls-files` by naming pattern, not a hardcoded list, so the six new test paths force no convention-guard edit",
    "package.json — read in full; D-027 is a two-line script change; grepped the repo for other 'npm run android' references (README.md, CLAUDE.md) and confirmed they name the script, not its value, so they are unaffected",
    "src/services/index.ts and src/models/index.ts — confirmed neither root barrel needs editing for D-031's deliberately-excluded sandbox/maintenance folders",
    "Compared ac_coverage[AC-001..AC-015] against surface_map for each AC's declared surface independently of the gate's own flag — all 15 sets match exactly, confirming zero surface drift" ],
  "guesses": [
    { "file": "src/features/settings/SettingsScreen.tsx",
      "question": "D-017 adds `isResettingSandbox: boolean` to useSettingsStore, but D-020's 'third section' and D-021's exact JSX for the reset row (a Pressable carrying only testID and style={styles.card}, wrapping one AppText label and one AppText caption) never mention it, and D-023's onPress only says to await resetSandbox() then call navigation.reset. Whether the Pressable should be disabled, show any busy indication, or guard against a second press while isResettingSandbox is true — or whether the flag is meant to go unconsumed by this screen — is left for me to invent." } ],
  "missing_files": [],
  "surface_adjudications": [] }

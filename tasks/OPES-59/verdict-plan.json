{ "schema": 1,
  "gate": "plan-judge",
  "subject": "plan.md",
  "subject_sha256": "4d9667f6722d76fd998e7ee795bc4ae15e339019cc443a58b32cc670408d738d",
  "judge_agent": "aif-plan-judge",
  "at": "2026-08-18T15:35:00Z",
  "checked": [
    "Re-read the full plan.md fresh (not diffed against memory) and confirmed the counts the prompt claims: spec_sha256 e8f47282c669510e9555fa6701d004d02e41975655f9b8e94be206979fd90851 matches the approved spec.md's own subject_sha256 in ledger.json (seq 12/13); risk high; 12-file manifest (5 create + 7 change); 6 test files; 15 AC covered",
    "Compared ac_coverage[AC-001..AC-015] against surface_map for each AC's own declared surface, independently of the gate: all 15 sets match exactly (env surface = AC-001; database surface = AC-002/003; serviceInstance surface = AC-004/005/006; sandbox 4-file surface = AC-007/008/009/010/012/013; settings 2-file surface = AC-011/014/015) — zero drift, consistent with plan-form's report and with my prior pass",
    "D-017 (revised): confirmed the flag isResettingSandbox no longer appears anywhere in the decision text, replaced by a plain 'resetSandbox: () => Promise<void>' member with an explicit rejected note forbidding the flag's return",
    "D-032 (new): confirmed it closes the Pressable's prop set to exactly testID/style/onPress with an explicit 'because' (store exposes no busy flag) — settles what D-021 alone left open last round",
    "D-033 (new): confirmed it closes the press-guard question explicitly — no in-flight check, a second press re-runs the reset — grounded in AS-013 from spec.md, which does say a second run is the recovery path and reaches the same end state",
    "src/features/settings/state/useSettingsStore.ts (current) — read in full: today it has only isExporting + exportTransactions, already imports showBottomSheet/showErrorBottomSheet from the exact path D-019 needs, so D-017/D-018/D-019 add one clean member with no new import ambiguity",
    "src/features/settings/SettingsScreen.tsx and SettingsScreen.styles.ts (current) — read in full: confirmed styles.card/settingInfo/section already exist as D-022 claims, Pressable is not yet imported from react-native (trivial addition), and the two existing sections give D-020's 'third section' an unambiguous template to follow",
    "src/features/monobank/state/useMonobankStore.ts — read in full and confirmed disconnect() already does monobankTokenService.clear(), monobankAccountSelectionService.clear(), clearMonobankService(), useTransactionsStore.getState().reset(), and resets its own state to idle/defaults — this is exactly what D-017's 'because' clause (disconnect() returns the monobank and transactions stores to defaults, AS-016) requires, and its double-clearing of the two key-value singletons (already cleared once by resetSandboxEnvironment per D-014) is redundant but not ambiguous since both .clear() methods are plain unconditional deletes, safe to call twice",
    "src/features/transactions/state/useTransactionsStore.ts — read in full: reset() is a synchronous plain set() back to defaults (transactions:[], syncStatus:'idle', ...), confirming AS-016's 'in-memory transactions store state returns to its defaults' is reached through disconnect() with no separate edit needed",
    "src/services/monobank/serviceInstance.ts — read in full: current getMonobankService has no guard; D-009's guard is a clean addition before the cache check; confirmed MonobankError lives in ./types.ts (sibling file, no circularity) and MonobankErrorCode already includes 'FORBIDDEN'",
    "src/services/monobank/types.ts and index.ts — read in full: MonobankError's constructor is (code, message, retryAfterMs?), matching D-009's two-argument call; the barrel re-exports MonobankError from ./types, confirming the same-folder import D-009 needs is unambiguous and not the barrel (which would be circular from serviceInstance.ts's own folder)",
    "src/services/monobank/MonobankTokenService.ts and MonobankAccountSelectionService.ts — read in full: both .clear() methods are unconditional storage deletes (idempotent), and both already branch jest-vs-MMKV via the same pattern database.ts uses, confirming D-016's deep-import claim and D-014's ordering are safe",
    "src/shared/ui/bottom-sheet/index.ts — read in full: NotificationSheetConfig requires title/buttonTitle/onPress (message is optional); every existing call site in the repo (export catch, connect catch, sync catch, all three read this session) uses the identical buttonTitle:'OK', onPress:()=>{} pattern, so D-019's unspecified buttonTitle/onPress is not a genuine decision left open — it is copy of an exceptionless in-file convention, not a guess",
    "src/services/database/database.ts — read in full: the SQLite branch today builds exactly the object D-005 lists (schema, migrations, jsi, dbName:'opes', onSetUpError); D-007's 'require + return createSqliteAdapter(SQLiteAdapter)' is a direct, mechanical extraction of what's already there, confirmed line-for-line",
    "src/shared/env/env.ts, index.ts, react-native-config.d.ts — read in full and cross-checked against node_modules/react-native-config/index.d.ts: the base NativeConfig interface does carry '[name: string]: string | undefined', so Config.OPES_ENV typechecks under strict mode via the index signature even though only CLAUDE_API_KEY is explicitly declared — D-003's claim is accurate, D-001 will compile",
    "android/app/build.gradle — read in full: line 4 is the dotenv.gradle apply D-024 places its new line above; the react{} block's commented debuggableVariants example at line 25 is the exact block D-026 edits; both unchanged from the manifest's description",
    "package.json — read in full: android/ios scripts are still the plain 'react-native run-android'/'run-ios' D-027/D-028 describe editing/leaving alone",
    "src/app/navigation — grepped routes.ts/types.ts/RootNavigator.tsx: ROOT_ROUTES.WELCOME = 'Welcome' (matches AC-011's expected string) and SettingsScreenNavigationProp is a NativeStackNavigationProp (carries .reset() from the external dependency already declared), confirming D-023 forces no navigation-layer file",
    ".gitignore — read in full: .env / .env.* / !.env.example confirmed, validating D-029",
    "src/services/index.ts and src/models/index.ts — read in full: neither barrels sandbox/ or maintenance/ today (services/index.ts already omits several folders; models/index.ts lists five entities, none named maintenance), so D-031's 'add neither folder to the root barrels' forces no edit to either file",
    "src/stores/useUserStore.ts and src/features/welcome/WelcomeScreen.tsx — read in full: WelcomeScreen calls bootstrap() in a useEffect on every mount, and bootstrap() re-derives currentUserId/isCheckedIn from usersRepository.hasAnyUser()/findFirst() against whatever the users table currently holds — after navigation.reset remounts WelcomeScreen against a wiped table, bootstrap() creates a fresh unchecked-in user with no code change to useUserStore, confirming it stays outside the manifest correctly",
    "src/features/home/HomeScreen.tsx — grepped: loadCardsByUser is called from an effect keyed on currentUserId, giving useCardsStore the same self-healing shape as useUserStore, confirming it too needs no manifest entry",
    "test/setup.js — grepped the react-native-config mock: jest.mock('react-native-config', () => ({ __esModule: true, default: {} })) hands every test an empty Config object by default, matching AC-001's 'no OPES_ENV key' precondition and giving the plan's described mutation seam (Config.OPES_ENV = 'sandbox') something real to mutate",
    "Verified no test files exist yet under src/features/settings or src/services/sandbox (only the unrelated settings/utils.test.ts) — the six test paths in the manifest are genuinely new, so no already-written test locks in an interpretation the plan text does not itself state" ],
  "guesses": [],
  "missing_files": [],
  "surface_adjudications": [] }

{ "schema": 1,
  "gate": "spec-judge",
  "subject": "spec.md",
  "subject_sha256": "d7dc68e8490ea111fe27eff9cc4bc49b2264ae83bc741bca8facf408f0e0fc73",
  "judge_agent": "aif-spec-judge",
  "at": "2026-08-18T15:12:40Z",
  "pass": true,
  "checked": [
    "Rework item 1 (cards) against AC-008: 'a sandbox build whose database holds one card row created through the ordinary card-creation path' / 'the card row count equals zero' — the criterion the human asked for, seeded and asserted, matching the ticket's 'наповнити її можна лише руками'",
    "Rework item 2 (AS-016) against the rewritten AS-016 and the new VG-012 — the behavioral half ('the reset clears no key-value entry for filters') stayed an assumption, the unestablished half ('Nothing here establishes that no filter state is persisted outside the database … no test in this suite would go red') moved to verification_gaps; the human asked for one or the other and got the split",
    "Rework item 3 (VG-002) against AC-002 and the restated VG-002: AC-002 asserts dbName in the adapter options equals 'opes' for a non-sandbox build — the human's point 2, the irreversible one — and VG-002 now claims only 'SQLite … opens the file named opes on a device is WatermelonDB's contract', i.e. the human's point 3 alone",
    "The residue the human named ('database.ts takes the LokiJS branch under jest') against VG-003, which files it separately instead of leaving it inside VG-002",
    "The dropped criterion (OPES_ENV=sandbox → isSandboxBuild() returns true) for a surface left uncovered: surfaces[] entry 'src/shared/env — isSandboxBuild()' is still covered by AC-001 (absent key → false), and the enabling direction is realized transitively by the 'a sandbox build' given of AC-003, AC-004, AC-005 and AC-014, which VG-005 states is injected through OPES_ENV in the react-native-config jest mock rather than by stubbing isSandboxBuild()",
    "The fold of the old resolveDatabaseName() pair into AC-002/AC-003 against the ticket's hardest constraint, 'Ім'я файлу реальної бази мусить лишитись незмінним': the 'opes' literal is still asserted, one link later in the chain, and AS-019 ('reads dbName from a single build-flag-aware name resolver') makes the adapter-options assertion go red on any resolver regression — nothing the old pair protected is now unprotected",
    "Both halves of the ceiling trade against the spec's own prose 'Компроміс, який довелось зробити' — the drop and the fold are declared in the body, not made silently, so the human can rule on them",
    "Each of the 15 criteria for a single machine-checkable expect and for bundling: every 'then' carries exactly one assertion (a row count, a call count, a returned null, a testID element count, a raised error, a string value), so a failure is attributable to one id",
    "All 19 assumptions read for entries that are really limits of verification — AS-008 (runtime gating, not bundle exclusion), AS-010 (single choke point), AS-011 (older schema runs the migration list), AS-013 (reset is not atomic), AS-016 (filters), AS-018 (MMKV not namespaced in code) each state system behavior, and each has its non-verification counterpart filed separately as VG-009, VG-008, VG-007, VG-011, VG-012, VG-004",
    "All 12 verification_gaps read for the reverse mis-filing: each states what this cycle does not establish, none smuggles in a behavioral decision",
    "Every surface in surfaces[] for at least one covering criterion — env (AC-001), database (AC-002/003), monobank store (AC-004/006), transactions store (AC-005), sandbox service (AC-007/008/009/010/012/013), settings screen (AC-011/014/015): all six covered",
    "The ticket's 'Monobank у тестовій збірці' — 'ніколи й жодним шляхом' plus the five named triggers — against AC-004 and AC-005 (fetch call count zero), AS-009/AS-010 (choke point at getMonobankService) and VG-008, which records that the triggers are not each exercised",
    "The ticket's 'Скидання' paragraph against AC-007 (transactions), AC-008 (cards), AC-012 (users and the repeat run), AC-009 (token), AC-010 (account selection), AS-015 (onboarding flag is users.checked_in) and AC-011 (lands on the onboarding entry in the same session)",
    "AC-011's 'first route name equals Welcome' against the ticket's 'опиняється на екрані підключення' and against AS-014, which records the divergence and its reason rather than leaving it silent",
    "The ticket's 'Що видно у звичайній збірці' against AC-015 (no reset item), AC-002 (the normal build's adapter still gets dbName 'opes', so no sandbox file is opened) and AC-006 (connect still reaches the Monobank host)",
    "The ticket's 'Deliberately left open' list against the assumptions that resolve it: nomenclature (AS-001/002/004/006/007), isolation mechanism (AS-008), observation point for the absent call (AS-009), stale sandbox schema (AS-011), confirmation and atomicity (AS-012/AS-013)",
    "The ticket's native-configuration requirements — separate bundle id/applicationId, side-by-side install, home-screen label, one config setting both flag and identifier — against AS-004/AS-005 and VG-001, which the human already accepted as irreducible",
    "The ticket's 'Поза скиданням тестові дані зберігаються між запусками' against VG-006 (single-process jest does not establish it)",
    "The ticket's 'Чого ця зміна не робить' list against non_goals[]",
    "The prose body of spec.md against the meta block — the summary's claims about AC-008, AS-016/VG-012, AC-002/AC-003 and VG-002/VG-003 match what the machine-readable block actually contains"
  ],
  "findings": [] }

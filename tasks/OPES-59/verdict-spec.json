{ "schema": 1,
  "gate": "spec-judge",
  "subject": "spec.md",
  "subject_sha256": "49c93a4e376784612568b5e786f2e34fb781e150a99f4a1240cbf5e888200bd8",
  "judge_agent": "aif-spec-judge",
  "at": "2026-08-18T13:55:12Z",
  "pass": true,
  "checked": [
    "AC-001 and AC-002 against the ticket's 'Як вмикається' — the env-var flag through src/shared/env — and against AS-002, which fixes OPES_ENV=sandbox as the only enabling value",
    "AC-003 against the ticket's hardest constraint, 'Ім'я файлу реальної бази мусить лишитись незмінним' — non-sandbox resolves to 'opes', a value a test can assert",
    "AC-004 against 'окремий файл бази — та сама схема WatermelonDB, інший файл', and against AS-007 which names it",
    "AC-005 and AC-006 against 'Тестова збірка не звертається до api.monobank.ua — ніколи й жодним шляхом'; both assert a fetch-spy call count of zero, one per store action",
    "The ticket's five named sync triggers (pull-to-refresh on Home and Transactions, post-connect autosync, on-open autosync, silent background sync) against AS-009/AS-010 (single choke point at getMonobankService) and VG-007, which records that each trigger is not separately exercised",
    "AC-007 against 'Не змінює поведінку звичайної збірки жодним чином' — the normal build still reaches the Monobank host",
    "AC-008, AC-009, AC-010 against the ticket's 'Скидання' paragraph — database wiped, token disconnected, account selection cleared, each a separate criterion with its own expect",
    "AC-011's 'first route name equals Welcome' against the ticket's 'опиняється на екрані підключення', and against AS-014, which records the divergence and its reason rather than leaving it silent",
    "AC-012 against 'повторний запуск скидання має приводити застосунок у коректний стан'",
    "AC-013 against 'У налаштуваннях, лише в тестовій збірці' — the reset refuses in a non-sandbox build",
    "AC-014 and AC-015 against 'Що видно у звичайній збірці': present under the flag, element count zero without it",
    "Every surface in the meta surfaces[] list for at least one covering criterion — env, database, monobank store, transactions store, sandbox service, settings screen: all six covered",
    "Each of the 15 criteria for a single machine-checkable expect and for bundling — no criterion carries more than one assertion",
    "All 18 assumptions read for entries that are really limits of verification; AS-008 (runtime gating, not bundle exclusion), AS-011 (older schema runs the migration list), AS-013 (reset is not atomic) and AS-018 (MMKV not namespaced in code) each state system behavior and have their non-verification counterpart filed separately as VG-008, VG-006, VG-010 and VG-003",
    "The ticket's five 'Deliberately left open' items against the assumptions that resolve them: nomenclature (AS-001/002/004/006/007), isolation mechanism (AS-008), observation point for the absent network call (AS-009), stale sandbox schema (AS-011), confirmation and atomicity (AS-012/AS-013)",
    "The ticket's native-configuration requirements — separate bundle id/applicationId, side-by-side install, home-screen label, one config setting both flag and identifier — against AS-004/AS-005 and VG-001, which records that this cycle builds and installs nothing",
    "The ticket's 'Поза скиданням тестові дані зберігаються між запусками' against VG-005, which records that a single-process jest run does not establish it",
    "The ticket's 'Чого ця зміна не робить' list against non_goals[]",
    "The prose body of spec.md against the meta — the summary claims the same flag, file names, gaps and open decisions the machine-readable block carries"
  ],
  "findings": [] }

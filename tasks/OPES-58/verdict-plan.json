{ "schema": 1,
  "gate": "plan-judge",
  "subject": "plan.md",
  "subject_sha256": "eebd999b656e7d903ecb38aa584bc5697a011cd7455f1b0cd55da0f8ed81cff3",
  "judge_agent": "aif-plan-judge",
  "at": "2026-08-23T00:00:00Z",
  "checked": [
    "src/services/monobank/MonobankTokenService.ts — current sync KeyValueStorage/InMemoryKeyValueStorage/createDefaultStorage MMKV branch matches D-002's deletion target exactly; save/get/clear bodies match D-008..D-013's literal statements one-for-one.",
    "src/services/secret-storage/index.ts — confirms the barrel exports a named `secretStore` singleton constructed at module load via `new SecretStore()`, matching D-003's stated reason to avoid a top-level import and D-005's `const { secretStore } = require(...)` destructure.",
    "src/services/secret-storage/SecretStore.ts — get/set/delete are Promise<string|null>/Promise<void>/Promise<void>, structurally identical to D-001's SecretStorePort, so the real store assigns to the seam without a cast as claimed.",
    "src/services/secret-storage/keychainKeyStore.ts — confirms the `typeof jest !== 'undefined'` lazy-require pattern the plan says createDefaultSecretStore should mirror (D-003/D-004) already exists here as `resolveKeychainApi`/`createDefaultKeychainKeyStore`.",
    "src/services/secret-storage/encryptedStore.ts — confirms react-native-mmkv is reached only behind this module's own jest guard, backing the plan's external-surface note that MonobankTokenService reaches MMKV only indirectly through secret-storage now.",
    "src/features/monobank/types.ts — current MonobankStoreActions has `disconnect(): void` and `loadSavedToken(): string | null`; D-014/D-015's target signatures are the direct async versions, D-016's untouched connect/loadAccounts/toggleAccount confirmed present and unrelated to monobankTokenService.",
    "src/features/monobank/state/useMonobankStore.ts — connect/disconnect/loadSavedToken bodies read exactly as D-017/D-018 describe (save() unawaited at its stated line, clear()/get() unawaited, statement order as-is), so the await insertions are mechanical.",
    "src/features/transactions/state/useTransactionsStore.ts — syncFromMonobank already async, `const saved = monobankTokenService.get();` is the only line D-019 touches; interface signature `Promise<void>` is unaffected so no types.ts-equivalent edit is needed here.",
    "src/services/sandbox/resetSandboxEnvironment.ts — already an async function with `monobankTokenService.clear()` unawaited at the exact spot D-020 describes; wipeAllData -> token -> account-selection order confirmed.",
    "src/features/monobank/ConnectMonobankScreen.tsx — the loadSavedToken effect (lines 49-54) and the bare `onPress={disconnect}` (line 162) match D-021..D-023 verbatim; Button's onPress type is TypeScript-checked next.",
    "src/shared/ui/Button.tsx — onPress prop is declared `() => void`; a `() => Promise<void>` disconnect is structurally assignable to that under TS's void-return-type rule, so D-023 (leave onPress={disconnect} unchanged) does not break typecheck as the plan claims.",
    "src/features/home/HomeScreen.tsx — the loadSavedToken effect and the existing `.catch(() => {})` idiom two effects below (syncFromMonobank) confirm D-021's instruction and its stated precedent are both accurate.",
    "src/features/settings/state/useSettingsStore.ts — resetSandbox's try block and the `useMonobankStore.getState().disconnect()` call site match D-024 exactly; the surrounding catch/showErrorBottomSheet path is unchanged as claimed.",
    "src/services/monobank/CLAUDE.md — current Token storage section literally reads 'Device: MMKV. Jest: in-memory map.', confirming D-025's rewrite target and that AC-015's required substring 'secret-storage' is not present yet.",
    "src/services/database/CLAUDE.md — confirms the current MMKV-vs-Jest exemplar line points at MonobankTokenService.ts, matching D-026's repoint target.",
    "src/services/monobank/MonobankAccountSelectionService.ts — still has its own synchronous KeyValueStorage/MMKV-vs-Jest branch untouched by this plan, so D-026's claim that it 'still is' a valid exemplar after the repoint holds.",
    "src/services/monobank/index.ts — re-exports MonobankTokenService/monobankTokenService/MonobankCredentials by name only, with no signatures duplicated, so D-027 (leave this file alone) does not strand a stale type.",
    "src/features/monobank/index.ts — re-exports MonobankStoreActions as a type only (no duplicated signature), so it needs no edit despite matching a grep for the interface name.",
    "src/stores/CLAUDE.md — its useShallow example selects `loadSavedToken` as a hook-state field for illustration only, asserting nothing about its sync/async return type, so it is not a missing file.",
    "Grepped src/ for every remaining call site of monobankTokenService.get/save/clear, loadSavedToken and useMonobankStore...disconnect() — no consumer outside the plan's eight change-files and the four already-listed test files.",
    "Confirmed src/features/{monobank,transactions,settings,home}/CLAUDE.md do not exist, so no undocumented feature-level doc needs updating alongside the two CLAUDE.md files the plan already lists."
  ],
  "guesses": [],
  "missing_files": [],
  "surface_adjudications": []
}

{ "schema": 1,
  "gate": "plan-judge",
  "subject": "plan.md",
  "subject_sha256": "fac0df692258eaa2f44b218d7c8a6cbecaea25f7ef65bf0be528cd443b2d7bf0",
  "judge_agent": "aif-plan-judge",
  "at": "2026-08-25T00:00:00Z",
  "checked": [
    "D-022..D-026 read against the plan body's 'What is already done, and stays done' section: the section states plainly, not just in the decision list, that the eleven inherited tests (AC-001..AC-009, AC-013, AC-014) 'have no red-phase evidence in this cycle' and rest on the green gate plus round one's history — the cost is stated in prose, not only encoded in a decision id.",
    "Counted the eleven tests in src/services/monobank/MonobankTokenService.test.ts against the claimed criterion list: AC-001, AC-002, AC-008 (save), AC-003, AC-004, AC-005, AC-009 (get), AC-006, AC-007 (clear), AC-013 (tsc), AC-014 (CLAUDE.md) = 11, matching D-022's 'AC-001..AC-009, AC-013 and AC-014' exactly.",
    "D-023/D-024/D-025 cross-checked for tightness: D-025 forbids changing any assertion, expected value or test name and confines the precondition to the beforeEach hook; D-024 explicitly forbids presenting the precondition as satisfying any criterion ('no AC covers it'); confirmed ac_coverage and surface_map for AC-016 point only at package.json/yarn.lock, never at the test file, so no criterion is claimed to be covered by the hook.",
    "Read package.json's dependencies map directly: react-native-get-random-values is absent from both dependencies and devDependencies today, so a beforeEach precondition gated on its presence is genuinely red against the current tree, and D-010/D-012 (yarn add into dependencies) are the only edits in this plan that would flip it green — matches D-023's claim that it 'is red now and greens exactly when this round's work lands.'",
    "Confirmed the beforeEach the plan means is the single top-level `beforeEach` at line 122 of the test file, which runs before every `it` in the file (all three describe blocks), so extending it reaches all eleven tests as D-022/D-023 describe, not a subset.",
    "Read src/services/secret-storage/cryptoKey.ts: line 45 is exactly the module-scope capture the plan names, and generateKey's existing guard/draw is exactly what D-002 says survives unmoved — AC-010/AC-011 remain fully specified (line-level instruction, verbatim message, explicit no-cache rule in D-003).",
    "Read index.js: current first import is `react-native` from AppRegistry; matches the plan's description of what D-008 must reorder, no ambiguity in the target shape.",
    "Read src/services/monobank/MonobankTokenService.ts: constructor, key names, and get/save/clear bodies already match what D-019 and the test file assert, confirming 'leave it exactly as previous round committed' is consistent with the tree the implementer will see.",
    "Read src/services/monobank/CLAUDE.md and src/services/secret-storage/CLAUDE.md: the Token storage / Key generation bullets are exactly where D-014..D-018 and AC-014/AC-015 point, nothing left for the implementer to invent about location or wording target.",
    "Read .aif/project.json: plan_files_max is 12; the manifest (0 create + 7 change + 2 tests = 9) is within it, unchanged from prior rounds.",
    "Cross-checked ac_coverage against spec.md's 16 acceptance ids: all of AC-001..AC-016 present, each resolving to a real file in files.create/files.change, uncovered is empty.",
    "Cross-checked surface_map's 9 keys against spec.md's 9 declared surfaces: same 9 names, each resolving to real files.",
    "Compared ac_coverage[<AC>] to surface_map[<its surface>] for all 16 criteria one by one: every pair is the identical file set (including AC-013's two-file set and AC-016's two-file set), so no criterion is flagged for surface drift and surface_adjudications is correctly empty."
  ],
  "guesses": [],
  "missing_files": [],
  "surface_adjudications": [] }

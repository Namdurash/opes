{ "schema": 1,
  "gate": "plan-judge",
  "subject": "plan.md",
  "subject_sha256": "8b92a0377a9133e5189071926e479fd9cb4ff579ada47f5574a2fd347d903993",
  "judge_agent": "aif-plan-judge",
  "at": "2026-08-25T00:00:00Z",
  "checked": [
    "src/services/secret-storage/cryptoKey.ts: read the live file — line 45 is exactly the capture D-001 names. D-002 gives the identical replacement expression verbatim as generateKey's first statement, D-003 forbids any cache/memoized resolver, D-004 pins the exported signature `(): GeneratedKey`, D-005 pins the thrown message byte-for-byte, D-006 pins everything else (KEY_BYTE_LENGTH, BASE64_ALPHABET, CryptoLike, toBase64, eslint pragmas) as unchanged. No decision left open here.",
    "index.js: read the live file (5 lines: AppRegistry/react-native, App, app.json imports, then registerComponent). D-008 pins the polyfill import as the literal first statement above the existing three; D-009 pins the docblock rule (rationale in the existing top docblock, no second quoted occurrence of the package name). Nothing to invent.",
    "package.json: read the live file — react-native-get-random-values is absent from both dependencies and devDependencies today. D-010 pins the install command (`yarn add react-native-get-random-values`, no hand-written version range), D-011 pins the lockfile discipline, D-012 pins the dependencies map (not devDependencies). No guess required.",
    "yarn.lock: confirmed it is a yarn v1 classic lockfile ('# yarn lockfile v1' header). Yarn v1 never reads or writes package-lock.json and vice versa; searched the repo for any lockfile-sync mechanism (husky, lint-staged, postinstall script referencing package-lock.json) and found none outside node_modules and unrelated tasks/ records. `yarn add` therefore cannot touch package-lock.json in this repo, so the plan's D-011 premise holds and excluding package-lock.json from the manifest is correct, not a missing file.",
    "package-lock.json: confirmed present and tracked (git-visible at repo root), but confirmed it is not written by any yarn command per the check above — its absence from files.change is consistent with D-011's claim that it stays byte-identical.",
    "src/services/monobank/MonobankTokenService.ts: read the live file — save()/get()/clear() already match AC-001 through AC-009 exactly (both keys written/deleted, presence decided by the token key alone, no plaintext fallback, no try/catch so rejections propagate unchanged). Confirms D-019's claim that this file is already correct and the manifest entry is coverage-only, not an edit instruction.",
    "src/services/monobank/CLAUDE.md: read the live file — the 'Token storage' section already contains the literal text 'secret-storage', already satisfying AC-014 with no edit needed. Confirms D-019 for this file too.",
    "src/services/secret-storage/CLAUDE.md: read the live file — the current 'Key generation' bullet describes crypto.getRandomValues but does not mention react-native-get-random-values or per-call resolution. D-014 through D-018 pin the four facts the new prose must state (getRandomValues resolved per-call, device source is the named polyfill, polyfill must be index.js's first import, fail-closed with no Math.random fallback); AC-015 only checks for the package-name substring, so the exact prose is a documentation wording choice, not a behavior-affecting guess.",
    "tsconfig.json: include is **/*.ts and **/*.tsx only (node_modules and Pods excluded); index.js is a .js file and is therefore outside tsc's include set. Confirms the plan's claim that AC-013 cannot be broken by the index.js edit and that no @types declaration for react-native-get-random-values is needed for tsc to pass.",
    ".aif/project.json: the only 'required' check is 'yarn lint'; the test command runs jest only. Neither invokes a native build, pod install, or simulator — nothing in the frozen suite requires the iOS pods to be linked, which is the load-bearing fact behind D-013's exclusion of ios/Podfile.lock.",
    ".eslintrc.js: extends '@react-native' with only no-var and no-restricted-imports rules added; no import/no-unresolved or module-resolution rule that could fail differently with vs without linked native pods. `yarn lint` only needs the package present in node_modules (which `yarn add` provides), not linked natively.",
    "jest.config.js: grepped for any reference to index.js or a runner that would execute it — none found, confirming nothing in the frozen suite exercises index.js at runtime, matching the plan's VG-002 and D-013's premise that pods are irrelevant to the test suite going green.",
    "src/services/monobank/MonobankTokenService.test.ts: read the live file — it is still the pre-respec version (AC-001..AC-015 under the old numbering, includes the two types.ts-signature cases and the 'returns a Promise' case the new spec.md cut). This is a test file and, per the plan's own note, its renumbering is the test station's work; it does not appear in files.change and correctly does not, since the implementer is not the one who edits it.",
    "src/services/secret-storage/SecretStore.ts (read only, not edited): grepped generateKey/cryptoKey usage across src/ — SecretStore.ts imports `generateKey` from cryptoKey.ts and calls it as `this.generateKey()` with no argument, matching the unchanged `(): GeneratedKey` signature D-004 pins. Confirms D-007's claim that no other file under secret-storage/ needs an edit as a consequence of the cryptoKey.ts change.",
    "surface_map vs ac_coverage: compared the file set for every one of AC-001 through AC-016 against its surface's entry in surface_map. Every pair is set-identical (e.g. AC-013's ac_coverage {cryptoKey.ts, MonobankTokenService.ts} equals surface_map['npx tsc --noEmit'] exactly; AC-016's ac_coverage {package.json, yarn.lock} equals surface_map['package.json'] exactly). No criterion is flagged for surface drift, so no adjudication is due.",
    "Searched for a root-level react-native.config.js and any babel plugin wiring for native-module packages — none exists at the repo root (only inside individual node_modules packages). Confirms D-013's premise that autolinking needs no manual config file edit as part of this diff." ],
  "guesses": [],
  "missing_files": [],
  "surface_adjudications": [] }

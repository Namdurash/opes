{ "schema": 1,
  "gate": "spec-judge",
  "subject": "spec.md",
  "subject_sha256": "ac4f9383c4f362d996e6e02f3d87d39262a8bce47451ea019715a3200b65ef97",
  "judge_agent": "aif-spec-judge",
  "at": "2026-08-23T12:00:00Z",
  "pass": true,
  "checked": [
    "AC-001 and AC-002 against the ticket's 'save(token, clientName) writes both the token and the client name into the encrypted store, under the storage keys monobank_personal_token and monobank_client_name' — two keys, one assertion each, no bundling",
    "AC-003 against the ticket's 'Its get / save / clear become async' — instanceof Promise is assertable",
    "AC-004 and AC-005 against the ticket's 'get() resolves both values' — token and clientName split into one assertion each",
    "AC-006 against the ticket's edge case 'No token stored — get() resolves null rather than an object with empty fields'",
    "AC-007 against the ticket's edge case 'must not be papered over with a plaintext fallback read', and against AS-005",
    "AC-008 and AC-009 against the ticket's 'clear() removes both' — one key per criterion",
    "AC-010 and AC-011 against the ticket's 'When the underlying SecretStore rejects, the service does not swallow it — the rejection reaches the caller' and the edge case naming save()",
    "The dropped clear()-rejection criterion, filed as VG-004, judged against the ticket's rejection sentence and its Edge cases section (which names only the write case) — see reasoning in findings decision",
    "AC-012 and AC-013 against the ticket's surface 'src/features/monobank/types.ts — loadSavedToken and disconnect signatures'; both are assertable as type-level checks under the tsc gate of AC-014",
    "AC-014 against the ticket's 'npx tsc --noEmit passes' and its call-site list (useMonobankStore, useTransactionsStore.syncFromMonobank, HomeScreen.tsx, ConnectMonobankScreen.tsx)",
    "AC-015 against the ticket's 'The docs say where the token now lives' and the surface src/services/monobank/CLAUDE.md",
    "Every surface in the meta block's surfaces[] traced to at least one AC id",
    "The ticket's four 'What should be true after' claims traced forward into criteria; 'the existing suite stays green' noted as a Definition-of-Done gate rather than an uncovered surface",
    "AS-001 through AS-010 read individually for entries that are really limits of verification rather than statements of behaviour — AS-007 (ordering unspecified, not partial-write safe) and AS-009 (no status/ordering/failure-presentation change) examined most closely",
    "VG-001 through VG-005 read individually to confirm each is a limit of what this cycle establishes and not a behaviour claim",
    "non_goals[] against the ticket's 'Scope note — this ticket was split' to confirm OPES-63 and OPES-64 are excluded by the ticket itself, not by the spec narrowing scope on its own authority",
    "The spec's prose body against the ticket's Risk section"
  ],
  "findings": [] }

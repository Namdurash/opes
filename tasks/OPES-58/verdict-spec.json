{ "schema": 1,
  "gate": "spec-judge",
  "subject": "spec.md",
  "subject_sha256": "5ba2c7551b726ee16a2cce7ef010860e3fef4dcb185a2755f5a40c0f1d5e639f",
  "judge_agent": "aif-spec-judge",
  "at": "2026-08-25T00:00:00Z",
  "pass": true,
  "checked": [
    "AC-016 against the prior round's observation: it pins the dependencies map of package.json specifically excluding devDependencies, which is the one structural fact a test can read without a network or install step",
    "AC-016 against ticket point 2 ('Додати залежність react-native-get-random-values') — supported, not invented",
    "AC-016 well-formedness: single surface, single assertion, expect is a literal string a test can compare against a key lookup",
    "VG-003 against what AC-016 leaves open — it names node_modules installation, iOS/Android native linking and pod reinstall explicitly, so the remainder is not silently claimed",
    "AC id sequence AC-001..AC-016 for gaps and duplicates after the append",
    "Cross-references: VG-008 cites AC-013 (npx tsc --noEmit, correct), VG-002 cites AC-012 (index.js, correct), VG-003 cites AC-016 (correct)",
    "surfaces[] against acceptance[]: all nine surfaces including package.json have at least one criterion; no criterion names a surface absent from the list",
    "Closing narrative paragraph against the limit history: it now states the 15-limit applied at rewrite, names the four cuts, and states the raise to 16 for AC-016 and that the cuts are not restored",
    "AC-010 as a discriminator: a stub installed after module load with an expected call count of 1 goes red against a module-load capture of globalThis.crypto",
    "AC-011 as a discriminator: removing globalThis.crypto after module load must still throw, which a captured reference would not",
    "The four dropped criteria: 'save returns a Promise' (absorbed by AC-001/002/008 awaiting save), 'no token stored resolves null' (absorbed by the strictly stronger AC-005), and the two types.ts signature criteria (carried by AS-011 plus AC-013, with the residual limit filed as VG-008)",
    "assumptions[] scanned for entries that are really verification limits: AS-012 and AS-016 state behaviour/config decisions, and their unestablished consequences are filed separately as VG-003 and VG-001",
    "ticket.md 'Surfaces the change is seen through' and 'Edge cases that matter' against the criteria set",
    "ticket.md 'ПОПЕРЕДЖЕННЯ ДЛЯ СТАНЦІЇ SPEC' against AC-010/AC-011/AC-012 — no criterion merely calls generateKey() and observes success"
  ],
  "findings": [] }

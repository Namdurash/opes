{ "schema": 1,
  "gate": "spec-judge",
  "subject": "spec.md",
  "subject_sha256": "0139d3e14534194c97b612fda38c6c9e5a161202e974daf96ed0ebca90ef9836",
  "judge_agent": "aif-spec-judge",
  "at": "2026-08-25T09:41:07Z",
  "pass": true,
  "checked": [
    "All 15 acceptance criteria read individually for a single assertable expect; none bundles two checks, every expect is a literal string, null, or number a test can compare against",
    "AC-010 against src/services/secret-storage/cryptoKey.ts line 45 (const cryptoSource = (globalThis as { crypto?: CryptoLike }).crypto): pre-fix the module-load capture means a stub installed after import is never called, so the count is 0 and the criterion is red; post-fix a call-time lookup makes it 1 — not vacuous",
    "AC-011 against the same line plus the throw on line 53-55: pre-fix removing globalThis.crypto after import leaves the captured source intact and generateKey succeeds, so 'throws' is red; post-fix the lazy lookup throws 'No cryptographically secure random source is available.', which does contain the expect text 'random source' — not vacuous",
    "Confirmed neither crypto criterion is of the form the ticket warned about (calling generateKey() and observing success under Node's own globalThis.crypto); both pin the module-load-capture-vs-lazy-lookup difference structurally",
    "AC-012 against the current repository-root index.js, whose first import statement is from 'react-native' — the criterion is red today and testable by reading the file",
    "Dropped criterion 'save returns a Promise instance' against AC-008: a synchronous save could not throw for a set() that rejects asynchronously, so AC-008 already forces save to return the chained promise; AC-009 does the same for get. Genuinely absorbed, no coverage lost",
    "Dropped criterion 'no token stored resolves null' against AC-005: AC-005's given is the same absent-token condition plus a plaintext copy under the same key, and any implementation returning null for it also returns null for the plain case, so AC-005 is strictly stronger. Absorption is real",
    "Dropped types.ts signature criteria against src/features/monobank/types.ts, which already declares disconnect(): Promise<void> and loadSavedToken(): Promise<string | null>; with those declarations in place a sync void implementation is not assignable and AC-013's tsc exit code 0 does enforce them. The residual is filed as VG-008 rather than left silent",
    "Ticket surfaces (MonobankTokenService get/save/clear, types.ts, the call sites, src/services/monobank/CLAUDE.md) mapped onto criteria: AC-001..AC-009, AC-013, AC-014 — none uncovered",
    "The five reworked ticket items mapped onto the spec: partial non-goal lift in non_goals, polyfill import in AC-012 and AS-013, lazy lookup in AC-010/AC-011 and AS-014, secret-storage/CLAUDE.md in AC-015, real-device success in VG-001",
    "AC-005 against the ticket's third edge case ('must not be papered over with a plaintext fallback read') — the criterion asserts exactly that, no contradiction",
    "AS-001..AS-017 each read for gap-flavoured content: all state how the system or the change behaves, none says the suite fails to exercise something. AS-011's coverage remark is mirrored in VG-008 and AS-012's in VG-003, so the gaps sit in verification_gaps where they belong",
    "VG-001..VG-008 each read: all eight are statements about what this cycle will not establish (device RNG, index.js at runtime, package install, device storage, upgrade path, call-site ordering, clear() rejection, types.ts transitive coverage) — correctly filed",
    "src/services/secret-storage/CLAUDE.md as it stands today: it documents crypto.getRandomValues and contains no mention of react-native-get-random-values, so AC-015 is red before the change"
  ],
  "findings": [] }

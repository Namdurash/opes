{ "schema": 1,
  "gate": "plan-judge",
  "subject": "plan.md",
  "subject_sha256": "4be7a7750bb3b89293a16f940f28030afb07a655c785a9fb0df8cd22ab88f50e",
  "judge_agent": "aif-plan-judge",
  "at": "2026-08-18T12:00:00Z",
  "checked": [
    "scripts/testConventionGuard.ts (create): D-001..D-006 give exact export names/signatures (findTestConventionViolations, isTestFile) and a precise, mechanically-implementable classification rule (dir-segment check, infix check, allowed-location check) that covers every AC-001..AC-010 given/when/then without leaving a free decision.",
    "scripts/checkTestConvention.ts (create): D-007..D-010 give the exact git invocation (execSync('git ls-files', {encoding:'utf8'})), the exact failure/exit-code rules for git failure, zero-test-found, violations-found and clean-tree, and the exact stdout contract used by AC-011..AC-015.",
    "package.json (change): read the real file — current \"lint\" script is \"eslint .\"; D-011's target string \"eslint . && tsx scripts/checkTestConvention.ts\" is an unambiguous literal replacement, and tsx is already a devDependency so no new dependency is implied.",
    "__tests__/App.test.tsx (read, pre-existing violation): read the actual file — its only relative imports are '../App' and '../src/stores/useUserStore', which is exactly what D-012's rewrite rule (../App -> ./App, ../src -> ./src) covers; no other relative import exists that the rule would miss.",
    "App.tsx exists at repo root (confirmed via glob) so the relocated test's './App' import resolves; no root App.test.tsx pre-exists, so there is no collision.",
    "jest.config.js: no testMatch/roots override is set (react-native jest-preset.js also sets no testMatch), so jest's default testMatch (**/?(*.)+(spec|test).[tj]s?(x)) already picks up a root-level App.test.tsx without any jest config edit — D-012's relocation does not force a jest.config.js change.",
    "tsconfig.json: include is **/*.ts, **/*.tsx with node_modules/Pods excluded, and @types/node is present in node_modules — the new scripts/*.ts files compile under the existing config without a tsconfig edit.",
    ".eslintrc.js: root: true, extends '@react-native' only, no ignore patterns keyed to __tests__ or scripts/ — removing __tests__ and adding scripts/checkTestConvention.ts, scripts/testConventionGuard.ts does not force an eslint config edit.",
    ".aif/project.json: test.roots is [\"__tests__\"], which D-012's directory removal would make stale, but spec.md's own VG-006 explicitly defers \"test.roots switch in .aif/project.json\" to later manual work outside this cycle's machine scope, and no AC in this plan depends on that field — not forced by this plan.",
    "Repo-wide grep for '__tests__' outside node_modules/tasks/.aif confirms no other config (metro, babel, package.json fields) references the __tests__ directory by path, so removing it does not force an edit anywhere else.",
    "surface_map vs ac_coverage diff: 'test-convention guard function' surface and AC-001..AC-010 coverage are identical sets (no drift). 'test-convention guard CLI' surface is {scripts/checkTestConvention.ts, package.json} but AC-011..AC-015 each cover only {scripts/checkTestConvention.ts} — flagged, adjudicated below.",
    "AC-011..AC-015 given/when text re-read: all five say only \"the guard CLI runs\", never mention npm run lint or the package.json wiring; the CLI's own exit-code/stdout behavior is fully producible and verifiable by invoking scripts/checkTestConvention.ts directly, so the narrower ac_coverage does not under-test what the criterion asserts." ],
  "guesses": [],
  "missing_files": [],
  "surface_adjudications": [
    { "ac": "AC-011", "verdict": "intended", "why": "the criterion tests the CLI script's own exit code on a clean git tree, not the npm run lint wiring, so package.json is irrelevant to satisfying it." },
    { "ac": "AC-012", "verdict": "intended", "why": "the criterion tests the CLI's exit code given a violating tracked path, which is fully determined by checkTestConvention.ts alone." },
    { "ac": "AC-013", "verdict": "intended", "why": "the criterion tests stdout content from running the CLI directly, not from the lint script's output, so package.json plays no verifying role." },
    { "ac": "AC-014", "verdict": "intended", "why": "the empty-test-tree exit-code behavior is produced entirely inside checkTestConvention.ts; the lint wiring is not exercised by this given/when." },
    { "ac": "AC-015", "verdict": "intended", "why": "the git-unavailable failure path is internal to the CLI's execSync call and its exit code, independent of how npm run lint invokes it." } ] }

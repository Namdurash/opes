{ "schema": 1,
  "gate": "plan-judge",
  "subject": "plan.md",
  "subject_sha256": "e47bb4b2ba76acfe71a579991193d07a5aaa455cce71b384019461047b4d91d7",
  "judge_agent": "aif-plan-judge",
  "at": "2026-08-18T12:15:00Z",
  "checked": [
    "scripts/testConventionGuard.ts (create): D-001 gives the exact export names/signatures (findTestConventionViolations(paths: string[]): string[], isTestFile(path: string): boolean). Wrote a candidate implementation from D-002..D-006 alone (reserved-dir check via D-002, infix check + allowed-form check via D-003/D-004, never-flag-non-test via D-005, POSIX split-only via D-006) and traced it against every given/when/then in AC-001..AC-010 one by one — each produces the exact expected violations-list length or membership with no invented rule.",
    "scripts/checkTestConvention.ts (create): D-007 gives the exact git invocation (execSync('git ls-files', { encoding: 'utf8' })) and the split/filter-empty-lines rule; D-008..D-010 give the exact exit-code rule for every branch (git failure, zero test files via isTestFile, violations present with each printed on its own stdout line, otherwise exit 0). Confirmed the branches cannot conflict: every violation is by construction a test file (D-002's reserved-dir class and D-003's infix class both imply isTestFile=true), so the D-009 zero-test check and the D-010 violations check never have to arbitrate a case the plan left silent.",
    "package.json (change): read the live file — current \"lint\" script is exactly \"eslint .\" (line 8) and \"tsx\" is already a devDependency (line 62) — D-011's replacement string \"eslint . && tsx scripts/checkTestConvention.ts\" is a literal, unambiguous edit that introduces no new dependency.",
    "Searched for other places that invoke eslint directly and would bypass the newly-wired lint script: no .github/workflows directory, no .husky directory, no lint-staged/husky/precommit config anywhere outside node_modules, and package.json's other scripts (android, ios, format, format:check, start, test, agent:dev) do not call eslint — nothing else needs to change in lockstep with D-011.",
    "tsconfig.json: include is **/*.ts / **/*.tsx (node_modules, Pods excluded) so the two new scripts/*.ts files compile under the existing config with no edit; node_modules/@types/node/index.d.ts is physically present (confirmed via glob and via package-lock.json's transitive @types/node entries), so the explicit `import { execSync } from 'node:child_process'` in checkTestConvention.ts resolves without adding @types/node to package.json or to compilerOptions.types.",
    ".eslintrc.js (read, not in the plan's list): root: true, extends '@react-native', with overrides keyed to src/**, App.tsx, index.js and to **/*.test.ts(x)/test/**/jest.config.js — nothing keys off scripts/ or off __tests__/__mocks__ path segments, so adding scripts/testConventionGuard.ts and scripts/checkTestConvention.ts, or the tree no longer containing __tests__/__mocks__, does not force an .eslintrc.js edit.",
    "jest.config.js: testMatch is '**/*.test.ts?(x)' with no location restriction (the file's own comment says this is deliberate — the guard is what enforces location, not jest), so it already collects the frozen scripts/testConventionGuard.test.ts and scripts/checkTestConvention.test.ts without an edit; modulePathIgnorePatterns already excludes .claude/worktrees.",
    ".aif/project.json: test.roots is already [\"test\"] (not the stale [\"__tests__\"] the earlier plan iteration would have left behind), confirming the manual half's step 5 is committed; its \"lint\" check already runs \"yarn lint\", which resolves through package.json's lint script, so D-011's change is picked up automatically with no project.json edit.",
    "Verified D-012's empirical premise (tracked tree already convention-clean) directly: globbed for __tests__, __mocks__, *.spec.ts, *.tests.ts, *.test.js outside node_modules — the only hits are under node_modules/ (gitignored, so absent from git ls-files) and .claude/worktrees/... (jest.config.js's own comment states .git/info/exclude hides this path from git, so it is likewise absent from git ls-files). src/agents/subscriptionDetective/ now holds fixtures/, not __mocks__/, and App.test.tsx already sits at the repo root as a tracked path in the allowed form — the git-visible tree the CLI will enumerate has no violation, matching D-012.",
    "Confirmed the uncommitted working-tree changes shown in git status (.eslintrc.js, App.test.tsx, package.json, package-lock.json, TransactionsScreen.test.tsx, useTransactionsViewModel.test.tsx, yarn.lock) are all 'M' (content modifications to already-tracked paths), not additions/renames/deletions — git ls-files returns the same path set regardless, so they do not disturb D-012's premise, which is about path names, not content.",
    "surface_map vs ac_coverage diff, computed directly: 'test-convention guard function' surface equals AC-001..AC-010's ac_coverage exactly (no drift, no adjudication needed). 'test-convention guard CLI' surface is {scripts/checkTestConvention.ts, package.json} while AC-011..AC-015 each cover only {scripts/checkTestConvention.ts} — flagged, adjudicated below.",
    "Re-read AC-011..AC-015's given/when/then in spec.md and the surrounding prose: every one says only 'the guard CLI runs' and several givens describe a fixture git repository ('a git repository whose tracked files are only allowed-form test paths', 'include one violating test path', etc.) that is not the real project repository — a test built from that given could not run through 'npm run lint' against the real tree at all, it must invoke checkTestConvention.ts directly against the fixture. spec.md's own body text (the AC section) never mentions npm run lint or package.json; that phrase appears only in plan.md's own explanatory prose, describing a separate verification channel (the lint DoD gate at green phase) distinct from the AC-marked jest tests." ],
  "guesses": [],
  "missing_files": [],
  "surface_adjudications": [
    { "ac": "AC-011", "verdict": "intended", "why": "the given constructs a fixture git repo with only allowed-form paths and asserts the CLI process's own exit code, which checkTestConvention.ts alone determines; package.json's lint wiring is not reachable from that given and asserts nothing about it." },
    { "ac": "AC-012", "verdict": "intended", "why": "the exit-code-1 behavior on a violating tracked path is produced entirely by checkTestConvention.ts's own violation branch; nothing in the given/when invokes npm run lint or inspects package.json." },
    { "ac": "AC-013", "verdict": "intended", "why": "the criterion asserts stdout content from running the CLI against a fixture repo with two violating paths, which only checkTestConvention.ts's own print loop produces — the lint script wiring plays no role in what stdout contains." },
    { "ac": "AC-014", "verdict": "intended", "why": "the zero-test-file exit-code path is internal to checkTestConvention.ts (via isTestFile over the fixture's tracked paths); the given never mentions the lint script, and testing it requires a fixture repo npm run lint cannot be pointed at." },
    { "ac": "AC-015", "verdict": "intended", "why": "the git-unavailable failure is about the CLI's own execSync call and its catch-driven exit code, independent of whether the process was launched via tsx directly or via the lint script that wraps it." } ] }
